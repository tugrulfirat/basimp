"use strict";

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _regeneratorValues(e) { if (null != e) { var t = e["function" == typeof Symbol && Symbol.iterator || "@@iterator"], r = 0; if (t) return t.call(e); if ("function" == typeof e.next) return e; if (!isNaN(e.length)) return { next: function next() { return e && r >= e.length && (e = void 0), { value: e && e[r++], done: !e }; } }; } throw new TypeError(_typeof(e) + " is not iterable"); }
function _toConsumableArray(r) { return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArray(r) { if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r); }
function _arrayWithoutHoles(r) { if (Array.isArray(r)) return _arrayLikeToArray(r); }
function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i["return"]) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); } r ? i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n : (o("next", 0), o("throw", 1), o("return", 2)); }, _regeneratorDefine2(e, r, n, t); }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
var _React = React,
  useState = _React.useState,
  useRef = _React.useRef,
  useEffect = _React.useEffect,
  useCallback = _React.useCallback;
var BRAND = {
  bg: "#050505",
  surface: "#111111",
  surfaceHover: "#1A1A1A",
  border: "#222222",
  accent: "#FFFFFF",
  accentHover: "#EEEEEE",
  accentLight: "#FFFFFF22",
  text: "#F0F0F0",
  textMuted: "#AAAAAA",
  danger: "#FF4757",
  success: "#2ECC71"
};
var TOOLS = [{
  id: "select",
  icon: "➚",
  label: "Select Tool",
  shortcut: "V"
}, {
  id: "redact",
  icon: "▬",
  label: "Redact Tool",
  shortcut: "R"
}, {
  id: "blur",
  icon: "◈",
  label: "Blur Tool",
  shortcut: "B"
}, {
  id: "pixelate",
  icon: "⊞",
  label: "Pixelate Tool",
  shortcut: "P"
}, {
  id: "crop",
  icon: "⊡",
  label: "Crop Tool",
  shortcut: "C"
}, {
  id: "resize",
  icon: "⤡",
  label: "Resize Tool",
  shortcut: "S"
}, {
  id: "arrow",
  icon: "↗",
  label: "Arrow Tool",
  shortcut: "A"
}, {
  id: "text",
  icon: "T",
  label: "Text Tool",
  shortcut: "T"
}, {
  id: "draw",
  icon: "✎",
  label: "Draw Tool",
  shortcut: "D"
}];
var REDACT_COLORS = ["#000000", "#1A1A2E", "#FF4757", "#FFFFFF", "#FFD700"];
var toolColors = {
  select: "#6C63FF",
  redact: "#FF4757",
  blur: "#6C63FF",
  pixelate: "#F39C12",
  crop: "#2ECC71",
  arrow: "#FF4757",
  text: "#00BCD4",
  draw: "#FF9FF3",
  resize: "#6C63FF"
};

// Vignelli's 6 essential fonts (Free equivalents)
var VIGNELLI_FONTS = [{
  name: "Garamond",
  family: "'EB Garamond', serif"
}, {
  name: "Bodoni",
  family: "'Libre Bodoni', serif"
}, {
  name: "Century",
  family: "'Crimson Pro', serif"
}, {
  name: "Futura",
  family: "'Jost', sans-serif"
}, {
  name: "Times",
  family: "'Times New Roman', serif"
}, {
  name: "Helvetica",
  family: "'Inter', sans-serif"
}];

// ── Social media canvas presets ───────────────────────────────────────────
var SOCIALS = [{
  id: "instagram",
  label: "Instagram",
  color: "#E1306C",
  icon: React.createElement("svg", {
    width: 16,
    height: 16,
    viewBox: "0 0 24 24",
    fill: "currentColor"
  }, React.createElement("path", {
    d: "M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"
  })),
  sizes: [{
    label: "Post (Square)",
    w: 1080,
    h: 1080
  }, {
    label: "Portrait Post",
    w: 1080,
    h: 1350
  }, {
    label: "Story / Reel",
    w: 1080,
    h: 1920
  }, {
    label: "Landscape Post",
    w: 1080,
    h: 566
  }]
}, {
  id: "youtube",
  label: "YouTube",
  color: "#FF0000",
  icon: React.createElement("svg", {
    width: 16,
    height: 16,
    viewBox: "0 0 24 24",
    fill: "currentColor"
  }, React.createElement("path", {
    d: "M23.495 6.205a3.007 3.007 0 0 0-2.088-2.088c-1.87-.501-9.396-.501-9.396-.501s-7.507-.01-9.396.501A3.007 3.007 0 0 0 .527 6.205a31.247 31.247 0 0 0-.522 5.805 31.247 31.247 0 0 0 .522 5.783 3.007 3.007 0 0 0 2.088 2.088c1.868.502 9.396.502 9.396.502s7.506 0 9.396-.502a3.007 3.007 0 0 0 2.088-2.088 31.247 31.247 0 0 0 .5-5.783 31.247 31.247 0 0 0-.5-5.805zM9.609 15.601V8.408l6.264 3.602z"
  })),
  sizes: [{
    label: "Thumbnail",
    w: 1280,
    h: 720
  }, {
    label: "Channel Banner",
    w: 2560,
    h: 1440
  }, {
    label: "Shorts",
    w: 1080,
    h: 1920
  }, {
    label: "Community Post",
    w: 1080,
    h: 1080
  }]
}, {
  id: "linkedin",
  label: "LinkedIn",
  color: "#0A66C2",
  icon: React.createElement("svg", {
    width: 16,
    height: 16,
    viewBox: "0 0 24 24",
    fill: "currentColor"
  }, React.createElement("path", {
    d: "M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"
  })),
  sizes: [{
    label: "Post (Square)",
    w: 1080,
    h: 1080
  }, {
    label: "Post (Landscape)",
    w: 1200,
    h: 627
  }, {
    label: "Story",
    w: 1080,
    h: 1920
  }, {
    label: "Banner",
    w: 1584,
    h: 396
  }]
}, {
  id: "x",
  label: "X / Twitter",
  color: "#000000",
  icon: React.createElement("svg", {
    width: 14,
    height: 14,
    viewBox: "0 0 24 24",
    fill: "currentColor"
  }, React.createElement("path", {
    d: "M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-4.714-6.231-5.401 6.231H2.741l7.73-8.835L1.254 2.25H8.08l4.713 5.865zm-1.161 17.52h1.833L7.084 4.126H5.117z"
  })),
  sizes: [{
    label: "Post (Square)",
    w: 1080,
    h: 1080
  }, {
    label: "Post (Landscape)",
    w: 1600,
    h: 900
  }, {
    label: "Header",
    w: 1500,
    h: 500
  }, {
    label: "Card",
    w: 800,
    h: 418
  }]
}, {
  id: "tiktok",
  label: "TikTok",
  color: "#010101",
  icon: React.createElement("svg", {
    width: 14,
    height: 14,
    viewBox: "0 0 24 24",
    fill: "currentColor"
  }, React.createElement("path", {
    d: "M12.525.02c1.31-.02 2.61-.01 3.91-.02.08 1.53.63 3.09 1.75 4.17 1.12 1.11 2.7 1.62 4.24 1.79v4.03c-1.44-.05-2.89-.35-4.2-.97-.57-.26-1.1-.59-1.62-.93-.01 2.92.01 5.84-.02 8.75-.08 1.4-.54 2.79-1.35 3.94-1.31 1.92-3.58 3.17-5.91 3.21-1.43.08-2.86-.31-4.08-1.03-2.02-1.19-3.44-3.37-3.65-5.71-.02-.5-.03-1-.01-1.49.18-1.9 1.12-3.72 2.58-4.96 1.66-1.44 3.98-2.13 6.15-1.72.02 1.48-.04 2.96-.04 4.44-.99-.32-2.15-.23-3.02.37-.63.41-1.11 1.04-1.36 1.75-.21.51-.15 1.07-.14 1.61.24 1.64 1.82 3.02 3.5 2.87 1.12-.01 2.19-.66 2.77-1.61.19-.33.4-.67.41-1.06.1-1.79.06-3.57.07-5.36.01-4.03-.01-8.05.02-12.07z"
  })),
  sizes: [{
    label: "Video / Story",
    w: 1080,
    h: 1920
  }, {
    label: "Square Post",
    w: 1080,
    h: 1080
  }, {
    label: "Landscape",
    w: 1920,
    h: 1080
  }, {
    label: "Profile Photo",
    w: 200,
    h: 200
  }]
}, {
  id: "pinterest",
  label: "Pinterest",
  color: "#E60023",
  icon: React.createElement("svg", {
    width: 14,
    height: 14,
    viewBox: "0 0 24 24",
    fill: "currentColor"
  }, React.createElement("path", {
    d: "M12 0C5.373 0 0 5.372 0 12c0 5.084 3.163 9.426 7.627 11.174-.105-.949-.2-2.405.042-3.441.218-.937 1.407-5.965 1.407-5.965s-.359-.719-.359-1.782c0-1.668.967-2.914 2.171-2.914 1.023 0 1.518.769 1.518 1.69 0 1.029-.655 2.568-.994 3.995-.283 1.194.599 2.169 1.777 2.169 2.133 0 3.772-2.249 3.772-5.495 0-2.873-2.064-4.882-5.012-4.882-3.414 0-5.418 2.561-5.418 5.207 0 1.031.397 2.138.893 2.738a.36.36 0 0 1 .083.345l-.333 1.36c-.053.22-.174.267-.402.161-1.499-.698-2.436-2.889-2.436-4.649 0-3.785 2.75-7.262 7.929-7.262 4.163 0 7.398 2.967 7.398 6.931 0 4.136-2.607 7.464-6.227 7.464-1.216 0-2.359-.632-2.75-1.378l-.748 2.853c-.271 1.043-1.002 2.35-1.492 3.146C9.57 23.812 10.763 24 12 24c6.627 0 12-5.373 12-12S18.627 0 12 0z"
  })),
  sizes: [{
    label: "Standard Pin",
    w: 1000,
    h: 1500
  }, {
    label: "Square Pin",
    w: 1000,
    h: 1000
  }, {
    label: "Long Pin",
    w: 1000,
    h: 2100
  }, {
    label: "Story Pin",
    w: 1080,
    h: 1920
  }]
}, {
  id: "bluesky",
  label: "Bluesky",
  color: "#0085FF",
  icon: React.createElement("svg", {
    width: 16,
    height: 16,
    viewBox: "0 0 24 24",
    fill: "currentColor"
  }, React.createElement("path", {
    d: "M12 10.8c-1.087-2.114-4.046-6.053-6.798-7.995C2.566.944 1.561 1.266.902 1.565.139 1.908 0 3.08 0 3.768c0 .69.378 5.65.624 6.479.815 2.736 3.713 3.66 6.383 3.364.136-.02.275-.039.415-.056-.138.022-.276.04-.415.056-3.912.58-7.387 2.005-2.83 7.078 5.013 5.19 6.87-1.113 7.823-4.308.953 3.195 2.05 9.271 7.733 4.308 4.267-4.308 1.172-6.498-2.74-7.078a8.741 8.741 0 0 1-.415-.056c.14.017.279.036.415.056 2.67.297 5.568-.628 6.383-3.364.246-.828.624-5.79.624-6.478 0-.69-.139-1.861-.902-2.204-.659-.298-1.664-.62-4.3 1.24C16.046 4.748 13.087 8.687 12 10.8z"
  })),
  sizes: [{
    label: "Post (Square)",
    w: 1080,
    h: 1080
  }, {
    label: "Post (Landscape)",
    w: 1200,
    h: 675
  }, {
    label: "Banner",
    w: 3000,
    h: 1000
  }, {
    label: "Avatar",
    w: 400,
    h: 400
  }]
}];
function btnStyle(BRAND) {
  var disabled = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
  var primary = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  return {
    padding: "6px 12px",
    borderRadius: 7,
    fontSize: 12,
    fontWeight: 600,
    cursor: disabled ? "not-allowed" : "pointer",
    border: "1px solid ".concat(primary ? BRAND.accent : BRAND.border),
    background: primary ? BRAND.accent : "transparent",
    color: disabled ? BRAND.textMuted : primary ? "#000" : BRAND.text,
    opacity: disabled ? 0.4 : 1,
    transition: "all 0.15s",
    display: "flex",
    alignItems: "center",
    gap: 4
  };
}
function BasimpEditor() {
  var _layers$find, _layers$find2, _TOOLS$find;
  var canvasRef = useRef(null);
  var overlayRef = useRef(null);
  var fileInputRef = useRef(null);
  var _useState = useState(null),
    _useState2 = _slicedToArray(_useState, 2),
    image = _useState2[0],
    setImage = _useState2[1];
  var _useState3 = useState({
      w: 0,
      h: 0
    }),
    _useState4 = _slicedToArray(_useState3, 2),
    imageDimensions = _useState4[0],
    setImageDimensions = _useState4[1];
  var _useState5 = useState(true),
    _useState6 = _slicedToArray(_useState5, 2),
    isFit = _useState6[0],
    setIsFit = _useState6[1];
  var _useState7 = useState(100),
    _useState8 = _slicedToArray(_useState7, 2),
    displayZoom = _useState8[0],
    setDisplayZoom = _useState8[1];
  var _useState9 = useState(100),
    _useState0 = _slicedToArray(_useState9, 2),
    customZoom = _useState0[0],
    setCustomZoom = _useState0[1];
  var imageCache = useRef(new Map());
  var getCachedImage = useCallback(function (src) {
    if (!src) return null;
    if (imageCache.current.has(src)) return imageCache.current.get(src);
    var img = new Image();
    img.src = src;
    img.onload = function () {
      return renderAll();
    };
    imageCache.current.set(src, img);
    return img;
  }, []);
  var _useState1 = useState({
      w: 0,
      h: 0
    }),
    _useState10 = _slicedToArray(_useState1, 2),
    resizeDims = _useState10[0],
    setResizeDims = _useState10[1];
  var _useState11 = useState(true),
    _useState12 = _slicedToArray(_useState11, 2),
    aspectLock = _useState12[0],
    setAspectLock = _useState12[1];
  var _useState13 = useState("arrow"),
    _useState14 = _slicedToArray(_useState13, 2),
    tool = _useState14[0],
    setTool = _useState14[1];
  var _useState15 = useState([]),
    _useState16 = _slicedToArray(_useState15, 2),
    layers = _useState16[0],
    setLayers = _useState16[1]; // [{id, type, x, y, w, h, ...}]
  var _useState17 = useState(null),
    _useState18 = _slicedToArray(_useState17, 2),
    previewLayer = _useState18[0],
    setPreviewLayer = _useState18[1];
  var _useState19 = useState(null),
    _useState20 = _slicedToArray(_useState19, 2),
    selectedLayerId = _useState20[0],
    setSelectedLayerId = _useState20[1];
  var _useState21 = useState(null),
    _useState22 = _slicedToArray(_useState21, 2),
    baseImage = _useState22[0],
    setBaseImage = _useState22[1]; // HTMLImageElement — the source image
  var _useState23 = useState("classic"),
    _useState24 = _slicedToArray(_useState23, 2),
    arrowStyle = _useState24[0],
    setArrowStyle = _useState24[1];
  var _useState25 = useState(false),
    _useState26 = _slicedToArray(_useState25, 2),
    isDrawing = _useState26[0],
    setIsDrawing = _useState26[1];
  var _useState27 = useState({
      x: 0,
      y: 0
    }),
    _useState28 = _slicedToArray(_useState27, 2),
    startPos = _useState28[0],
    setStartPos = _useState28[1];

  // 🔄 Sync Canvas Internal Resolution with Logical Image Dimensions
  useEffect(function () {
    if (!canvasRef.current || imageDimensions.w === 0) return;
    var canvas = canvasRef.current;
    if (canvas.width !== imageDimensions.w || canvas.height !== imageDimensions.h) {
      canvas.width = imageDimensions.w;
      canvas.height = imageDimensions.h;
      renderAll(); // Redraw immediately when resolution changes
    }
  }, [imageDimensions]);
  var _useState29 = useState([]),
    _useState30 = _slicedToArray(_useState29, 2),
    history = _useState30[0],
    setHistory = _useState30[1];
  var _useState31 = useState("#000000"),
    _useState32 = _slicedToArray(_useState31, 2),
    redactColor = _useState32[0],
    setRedactColor = _useState32[1];
  var _useState33 = useState(20),
    _useState34 = _slicedToArray(_useState33, 2),
    brushSize = _useState34[0],
    setBrushSize = _useState34[1];
  var _useState35 = useState("#FF4757"),
    _useState36 = _slicedToArray(_useState35, 2),
    drawColor = _useState36[0],
    setDrawColor = _useState36[1];
  var _useState37 = useState(""),
    _useState38 = _slicedToArray(_useState37, 2),
    textInput = _useState38[0],
    setTextInput = _useState38[1];
  var _useState39 = useState(null),
    _useState40 = _slicedToArray(_useState39, 2),
    textPos = _useState40[0],
    setTextPos = _useState40[1];
  var _useState41 = useState(false),
    _useState42 = _slicedToArray(_useState41, 2),
    showTextBox = _useState42[0],
    setShowTextBox = _useState42[1];
  var _useState43 = useState("Paste an image or drop a file to start"),
    _useState44 = _slicedToArray(_useState43, 2),
    statusMsg = _useState44[0],
    setStatusMsg = _useState44[1];
  var _useState45 = useState(true),
    _useState46 = _slicedToArray(_useState45, 2),
    showDropZone = _useState46[0],
    setShowDropZone = _useState46[1];
  var _useState47 = useState(24),
    _useState48 = _slicedToArray(_useState47, 2),
    fontSize = _useState48[0],
    setFontSize = _useState48[1];
  var _useState49 = useState(VIGNELLI_FONTS[5].family),
    _useState50 = _slicedToArray(_useState49, 2),
    fontFamily = _useState50[0],
    setFontFamily = _useState50[1]; // Default to Helvetica (Inter)

  // 💎 Pro State 
  var _useState51 = useState(false),
    _useState52 = _slicedToArray(_useState51, 2),
    isPro = _useState52[0],
    setIsPro = _useState52[1];
  var _useState53 = useState(false),
    _useState54 = _slicedToArray(_useState53, 2),
    proModalOpen = _useState54[0],
    setProModalOpen = _useState54[1];
  var _useState55 = useState(""),
    _useState56 = _slicedToArray(_useState55, 2),
    inputProKey = _useState56[0],
    setInputProKey = _useState56[1];
  var _useState57 = useState(false),
    _useState58 = _slicedToArray(_useState57, 2),
    verifying = _useState58[0],
    setVerifying = _useState58[1];

  // 📦 Batch Mode State
  var _useState59 = useState([]),
    _useState60 = _slicedToArray(_useState59, 2),
    fileQueue = _useState60[0],
    setFileQueue = _useState60[1]; // Array of File objects
  var _useState61 = useState(false),
    _useState62 = _slicedToArray(_useState61, 2),
    isBatchProcessing = _useState62[0],
    setIsBatchProcessing = _useState62[1];

  // Sync sliders when layer is selected
  useEffect(function () {
    if (selectedLayerId) {
      var l = layers.find(function (x) {
        return x.id === selectedLayerId;
      });
      if (l) {
        if (l.radius) setBrushSize(l.radius);
        if (l.fontSize) setFontSize(l.fontSize);
        if (l.fontFamily) setFontFamily(l.fontFamily);
        if (l.color) {
          if (l.type === "redact") setRedactColor(l.color);else setDrawColor(l.color);
        }
      }
    }
  }, [selectedLayerId]);
  var updateSelectedLayer = function updateSelectedLayer(props) {
    if (!selectedLayerId) return;
    setLayers(function (prev) {
      return prev.map(function (l) {
        return l.id === selectedLayerId ? _objectSpread(_objectSpread({}, l), props) : l;
      });
    });
  };
  var arrowStartRef = useRef(null);
  var _useState63 = useState(null),
    _useState64 = _slicedToArray(_useState63, 2),
    resizeHandle = _useState64[0],
    setResizeHandle = _useState64[1]; // Which handle of the layer is being dragged?
  var _useState65 = useState(null),
    _useState66 = _slicedToArray(_useState65, 2),
    layerStartPos = _useState66[0],
    setLayerStartPos = _useState66[1]; // Original coords when drag starts

  // 💎 License Verification
  var verifyProKey = /*#__PURE__*/function () {
    var _ref = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee(key) {
      var resp, data, _t;
      return _regenerator().w(function (_context) {
        while (1) switch (_context.p = _context.n) {
          case 0:
            if (key) {
              _context.n = 1;
              break;
            }
            return _context.a(2);
          case 1:
            // Silent check
            setVerifying(true);
            _context.p = 2;
            _context.n = 3;
            return fetch("https://bimp.us/api/verify", {
              method: "POST",
              body: JSON.stringify({
                key: key
              }),
              headers: {
                "Content-Type": "application/json"
              }
            });
          case 3:
            resp = _context.v;
            _context.n = 4;
            return resp.json();
          case 4:
            data = _context.v;
            if (data.valid) {
              setIsPro(true);
              localStorage.setItem("bimp_pro_key", key);
              setStatusMsg("💎 Bimp.us Pro Activated!");
              setProModalOpen(false);
            } else {
              if (proModalOpen) alert("Invalid license key. Please check your purchase receipt.");
            }
            _context.n = 6;
            break;
          case 5:
            _context.p = 5;
            _t = _context.v;
            if (proModalOpen) alert("Verification server offline or SSL not ready. Try again in a minute.");
          case 6:
            _context.p = 6;
            setVerifying(false);
            return _context.f(6);
          case 7:
            return _context.a(2);
        }
      }, _callee, null, [[2, 5, 6, 7]]);
    }));
    return function verifyProKey(_x) {
      return _ref.apply(this, arguments);
    };
  }();
  useEffect(function () {
    var savedKey = localStorage.getItem("bimp_pro_key");
    if (savedKey) verifyProKey(savedKey);
  }, []);

  // 💎 Smart Redact (AI Face Blur)
  var _useState67 = useState(false),
    _useState68 = _slicedToArray(_useState67, 2),
    modelsLoaded = _useState68[0],
    setModelsLoaded = _useState68[1];
  var handleAutoRedact = /*#__PURE__*/function () {
    var _ref2 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2() {
      var MODEL_URL, detections, newLayers, _t2;
      return _regenerator().w(function (_context2) {
        while (1) switch (_context2.p = _context2.n) {
          case 0:
            if (isPro) {
              _context2.n = 1;
              break;
            }
            setProModalOpen(true);
            return _context2.a(2);
          case 1:
            if (image) {
              _context2.n = 2;
              break;
            }
            return _context2.a(2);
          case 2:
            setStatusMsg("🤖 AI is looking for faces...");
            _context2.p = 3;
            if (modelsLoaded) {
              _context2.n = 5;
              break;
            }
            MODEL_URL = "https://cdn.jsdelivr.net/npm/@vladmandic/face-api@1.7.12/model/";
            _context2.n = 4;
            return faceapi.nets.tinyFaceDetector.loadFromUri(MODEL_URL);
          case 4:
            setModelsLoaded(true);
          case 5:
            _context2.n = 6;
            return faceapi.detectAllFaces(canvasRef.current, new faceapi.TinyFaceDetectorOptions());
          case 6:
            detections = _context2.v;
            if (!(detections.length === 0)) {
              _context2.n = 7;
              break;
            }
            setStatusMsg("No faces found. Try manual redact.");
            return _context2.a(2);
          case 7:
            newLayers = _toConsumableArray(layers);
            detections.forEach(function (det) {
              var _det$box = det.box,
                x = _det$box.x,
                y = _det$box.y,
                width = _det$box.width,
                height = _det$box.height;
              newLayers.push({
                id: Date.now() + Math.random(),
                type: "blur",
                x: Math.round(x - 10),
                y: Math.round(y - 10),
                w: Math.round(width + 20),
                h: Math.round(height + 20),
                intensity: 20
              });
            });
            setLayers(newLayers);
            setHistory([].concat(_toConsumableArray(history), [layers]));
            setStatusMsg("\u2713 Auto-redacted ".concat(detections.length, " faces"));
            _context2.n = 9;
            break;
          case 8:
            _context2.p = 8;
            _t2 = _context2.v;
            console.error(_t2);
            setStatusMsg("AI Error. Check console.");
          case 9:
            return _context2.a(2);
        }
      }, _callee2, null, [[3, 8]]);
    }));
    return function handleAutoRedact() {
      return _ref2.apply(this, arguments);
    };
  }();
  var applyMockup = function applyMockup() {
    var type = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : "browser";
    if (!isPro) {
      setProModalOpen(true);
      return;
    }
    if (!image) return;
    saveHistory();
    var canvas = canvasRef.current;
    var w = imageDimensions.w,
      h = imageDimensions.h;
    if (type === "browser") {
      var headerH = 34;
      var newW = w + 0;
      var newH = h + headerH;
      var off = document.createElement("canvas");
      off.width = newW;
      off.height = newH;
      var offCtx = off.getContext("2d");

      // Browser Header Background
      offCtx.fillStyle = "#222222";
      offCtx.fillRect(0, 0, newW, headerH);

      // Traffic Lights
      var colors = ["#FF5F56", "#FFBD2E", "#27C93F"];
      colors.forEach(function (c, i) {
        offCtx.fillStyle = c;
        offCtx.beginPath();
        offCtx.arc(16 + i * 18, headerH / 2, 5, 0, Math.PI * 2);
        offCtx.fill();
      });
      offCtx.drawImage(baseImage, 0, headerH);
      var newSrc = off.toDataURL();
      var newImg = new Image();
      newImg.src = newSrc;
      newImg.onload = function () {
        setBaseImage(newImg);
        setImage(newSrc);
        setLayers(function (prev) {
          return prev.map(function (l) {
            if (l.type === "arrow") return _objectSpread(_objectSpread({}, l), {}, {
              y1: l.y1 + headerH,
              y2: l.y2 + headerH
            });
            if (l.type === "draw") return _objectSpread(_objectSpread({}, l), {}, {
              points: l.points.map(function (p) {
                return {
                  x: p.x,
                  y: p.y + headerH
                };
              })
            });
            return _objectSpread(_objectSpread({}, l), {}, {
              y: l.y + headerH
            });
          });
        });
        canvas.width = newW;
        canvas.height = newH;
        setImageDimensions({
          w: newW,
          h: newH
        });
        setStatusMsg("✨ Browser Frame Applied!");
      };
    }
  };
  var _useState69 = useState(null),
    _useState70 = _slicedToArray(_useState69, 2),
    cropSelection = _useState70[0],
    setCropSelection = _useState70[1]; // { x, y, w, h } in canvas coords
  var cropSelRef = useRef(null); // mirror of cropSelection — used inside getCropHandleAtPos
  var cropDragRef = useRef(null); // { mode, startX, startY, origCrop }
  var _useState71 = useState(false),
    _useState72 = _slicedToArray(_useState71, 2),
    socialOpen = _useState72[0],
    setSocialOpen = _useState72[1];
  var _useState73 = useState(null),
    _useState74 = _slicedToArray(_useState73, 2),
    activeSocial = _useState74[0],
    setActiveSocial = _useState74[1];
  var _useState75 = useState(false),
    _useState76 = _slicedToArray(_useState75, 2),
    saveAsOpen = _useState76[0],
    setSaveAsOpen = _useState76[1];
  var socialRef = useRef(null);
  var saveAsRef = useRef(null);
  var lastDrawPos = useRef(null);

  // Core Composition Engine
  var renderAll = useCallback(function () {
    var exporting = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
    if (!canvasRef.current) return;
    var canvas = canvasRef.current;
    var ctx = canvas.getContext("2d");

    // 1. Clear and Draw Base
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    if (baseImage) {
      ctx.drawImage(baseImage, 0, 0);
    } else {
      // If no base image but we have dimensions, clear to transparent/checkerboard handled by CSS
      ctx.clearRect(0, 0, canvas.width, canvas.height);
    }

    // 2. Draw Layers in order
    var allLayers = [].concat(_toConsumableArray(layers), _toConsumableArray(previewLayer ? [previewLayer] : []));
    allLayers.forEach(function (layer) {
      drawLayer(ctx, layer);
    });

    // 3. Selection Highlight & Handles
    if (!exporting && selectedLayerId && tool === "select") {
      var l = layers.find(function (l) {
        return l.id === selectedLayerId;
      });
      if (l) {
        ctx.save();
        ctx.strokeStyle = BRAND.accent;
        ctx.lineWidth = 2;
        ctx.setLineDash([4, 4]);
        var x, y, w, h;
        if (l.type === "arrow") {
          ctx.beginPath();
          ctx.moveTo(l.x1, l.y1);
          ctx.lineTo(l.x2, l.y2);
          ctx.stroke();
          // Arrow handles
          ctx.setLineDash([]);
          ctx.fillStyle = "#fff";
          [[l.x1, l.y1], [l.x2, l.y2]].forEach(function (_ref3) {
            var _ref4 = _slicedToArray(_ref3, 2),
              px = _ref4[0],
              py = _ref4[1];
            ctx.fillRect(px - 4, py - 4, 8, 8);
            ctx.strokeRect(px - 4, py - 4, 8, 8);
          });
        } else if (l.type === "text") {
          ctx.font = "bold ".concat(l.fontSize, "px Inter");
          var m = ctx.measureText(l.text);
          x = l.x - 4;
          y = l.y - l.fontSize;
          w = m.width + 8;
          h = l.fontSize + 8;
          ctx.strokeRect(x, y, w, h);
        } else if (l.type === "draw") {
          // No resize for freehand yet
        } else {
          x = l.x - 2;
          y = l.y - 2;
          w = l.w + 4;
          h = l.h + 4;
          ctx.strokeRect(x, y, w, h);
          // 8 Handles
          ctx.setLineDash([]);
          ctx.fillStyle = "#fff";
          var hh = [[l.x, l.y], [l.x + l.w / 2, l.y], [l.x + l.w, l.y], [l.x, l.y + l.h / 2], [l.x + l.w, l.y + l.h / 2], [l.x, l.y + l.h], [l.x + l.w / 2, l.y + l.h], [l.x + l.w, l.y + l.h]];
          hh.forEach(function (_ref5) {
            var _ref6 = _slicedToArray(_ref5, 2),
              hx = _ref6[0],
              hy = _ref6[1];
            ctx.fillRect(hx - 4, hy - 4, 8, 8);
            ctx.strokeRect(hx - 4, hy - 4, 8, 8);
          });
        }
        ctx.restore();
      }
    }
  }, [baseImage, layers, previewLayer, selectedLayerId, tool]);

  // Effect to trigger redraw
  useEffect(function () {
    renderAll();
  }, [renderAll]);
  var drawLayer = function drawLayer(ctx, layer) {
    ctx.save();
    if (layer.type === "redact") {
      ctx.fillStyle = layer.color || "#000";
      ctx.fillRect(layer.x, layer.y, layer.w, layer.h);
    } else if (layer.type === "blur") {
      applyBlur(ctx, layer.x, layer.y, layer.w, layer.h, layer.radius);
    } else if (layer.type === "pixelate") {
      applyPixelate(ctx, layer.x, layer.y, layer.w, layer.h, layer.radius);
    } else if (layer.type === "arrow") {
      drawArrowOnCanvas(ctx, layer.x1, layer.y1, layer.x2, layer.y2, layer.color, layer.size, layer.style);
    } else if (layer.type === "text") {
      ctx.font = "bold ".concat(layer.fontSize, "px ").concat(layer.fontFamily || VIGNELLI_FONTS[5].family);
      ctx.fillStyle = layer.color;
      ctx.shadowColor = "rgba(0,0,0,0.6)";
      ctx.shadowBlur = 4;
      ctx.fillText(layer.text, layer.x, layer.y);
    } else if (layer.type === "draw") {
      if (layer.points && layer.points.length > 0) {
        ctx.beginPath();
        ctx.strokeStyle = layer.color;
        ctx.lineWidth = layer.size;
        ctx.lineCap = "round";
        ctx.lineJoin = "round";
        ctx.moveTo(layer.points[0].x, layer.points[0].y);
        layer.points.forEach(function (p) {
          return ctx.lineTo(p.x, p.y);
        });
        ctx.stroke();
      }
    } else if (layer.type === "image" && layer.src) {
      var img = layer.img instanceof HTMLImageElement ? layer.img : getCachedImage(layer.src);
      if (img && img.complete && img.naturalWidth !== 0) {
        ctx.drawImage(img, layer.x, layer.y, layer.w, layer.h);
      }
    }
    ctx.restore();
  };

  // Effect to measure actual canvas scale for the zoom slider UI
  useEffect(function () {
    if (!canvasRef.current || imageDimensions.w === 0) return;
    var updateZoom = function updateZoom() {
      var canvas = canvasRef.current;
      var rect = canvas.getBoundingClientRect();
      if (isFit) {
        setDisplayZoom(Math.max(1, Math.round(rect.width / imageDimensions.w * 100)));
      } else {
        setDisplayZoom(customZoom);
      }
    };

    // Use ResizeObserver for more reliable zoom updates without the "flicker" of setTimeout
    var observer = new ResizeObserver(updateZoom);
    observer.observe(canvasRef.current);
    updateZoom();
    return function () {
      return observer.disconnect();
    };
  }, [isFit, customZoom, imageDimensions.w, imageDimensions.h]);

  // Global hotkeys (Enter/Esc) for Apply/Cancel
  useEffect(function () {
    var handleKeyDown = function handleKeyDown(e) {
      if (e.key === "Enter") {
        if (tool === "resize") {
          e.preventDefault();
          applyResize();
        } else if (tool === "crop" && cropSelRef.current) {
          e.preventDefault();
          applyCrop();
        }
      } else if (e.key === "Escape") {
        if (tool === "resize") {
          e.preventDefault();
          selectTool("arrow");
        } else if (tool === "crop" && cropSelRef.current) {
          e.preventDefault();
          cancelCrop();
        }
      }
    };
    window.addEventListener("keydown", handleKeyDown);
    return function () {
      return window.removeEventListener("keydown", handleKeyDown);
    };
  }, [tool, resizeDims, imageDimensions, aspectLock]);

  // Curated presets that look great on any screen (light & dark)
  var COLOR_PRESETS = ["#FF4757",
  // red
  "#FF6B35",
  // orange
  "#FFD700",
  // yellow
  "#2ECC71",
  // green
  "#00BCD4",
  // cyan
  "#6C63FF",
  // purple (brand)
  "#FF9FF3",
  // pink
  "#FFFFFF",
  // white
  "#000000" // black
  ];
  var status = function status(msg) {
    var duration = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 2500;
    setStatusMsg(msg);
    if (duration) setTimeout(function () {
      return setStatusMsg("");
    }, duration);
  };
  var saveHistory = useCallback(function () {
    setHistory(function (h) {
      return [].concat(_toConsumableArray(h.slice(-19)), [JSON.stringify(layers)]);
    });
  }, [layers]);
  var undo = useCallback(function () {
    if (history.length === 0) return;
    var last = history[history.length - 1];
    try {
      var prevLayers = JSON.parse(last);
      setLayers(prevLayers);
      setHistory(function (h) {
        return h.slice(0, -1);
      });
      status("↩ Undo applied");
    } catch (e) {
      console.error("Undo failed", e);
    }
  }, [history]);
  var addLayerImage = function addLayerImage(src) {
    var img = new Image();
    img.src = src;
    img.onload = function () {
      var canvas = canvasRef.current;
      var w = img.width,
        h = img.height;
      // Scale down if larger than canvas
      var maxW = canvas.width * 0.8,
        maxH = canvas.height * 0.8;
      if (w > maxW || h > maxH) {
        var ratio = Math.min(maxW / w, maxH / h);
        w *= ratio;
        h *= ratio;
      }
      var newLayer = {
        id: Date.now(),
        type: "image",
        img: img,
        src: src,
        x: (canvas.width - w) / 2,
        y: (canvas.height - h) / 2,
        w: Math.round(w),
        h: Math.round(h),
        ratio: img.width / img.imgHeight || img.width / img.height
      };
      saveHistory();
      setLayers(function (prev) {
        return [].concat(_toConsumableArray(prev), [newLayer]);
      });
      setSelectedLayerId(newLayer.id);
      setTool("select");
      status("+ Image layer added: ".concat(Math.round(w), "\xD7").concat(Math.round(h), "px"));
    };
  };
  var handleFile = function handleFile(file) {
    if (!file) return;
    var reader = new FileReader();
    reader.onload = function (ev) {
      var img = new Image();
      img.onload = function () {
        if (!image) {
          // If no base image, set it
          setBaseImage(img);
          setImage(ev.target.result);
          setImageDimensions({
            w: img.width,
            h: img.height
          });
          setResizeDims({
            w: img.width,
            h: img.height
          });
          setIsFit(true); // Default to fit
          setLayers([]);
          setHistory([]);
          setShowDropZone(false);
          status("\u2713 Loaded: ".concat(file.name, " (").concat(Math.round(file.size / 1024), "KB)"));
        } else {
          // Otherwise, add as a new layer
          addLayerImage(ev.target.result);
        }
      };
      img.src = ev.target.result;
    };
    reader.readAsDataURL(file);
  };
  var handleFiles = function handleFiles(files) {
    var fileList = Array.from(files).filter(function (f) {
      return f.type.startsWith("image/");
    });
    if (fileList.length === 0) return;

    // Always load the first one immediately
    handleFile(fileList[0]);
    if (fileList.length > 1) {
      if (!isPro) {
        setStatusMsg("Pro Tip: Multi-file selection requires Bimp.us Pro.");
        setProModalOpen(true);
        return;
      }
      setFileQueue(fileList);
    }
  };
  var handleFileInput = function handleFileInput(e) {
    return handleFiles(e.target.files);
  };
  var handleDrop = function handleDrop(e) {
    e.preventDefault();
    handleFiles(e.dataTransfer.files);
  };
  var loadImage = function loadImage(src) {
    if (image) {
      addLayerImage(src);
      return;
    }
    var img = new Image();
    img.src = src;
    img.onload = function () {
      setBaseImage(img);
      setImage(src);
      setImageDimensions({
        w: img.width,
        h: img.height
      });
      setResizeDims({
        w: img.width,
        h: img.height
      });
      setIsFit(true);
      var canvas = canvasRef.current;
      if (canvas) {
        canvas.width = img.width;
        canvas.height = img.height;
      }
      setLayers([]);
      setShowDropZone(false);
      status("\uD83D\uDDBC Image loaded: ".concat(img.width, "\xD7").concat(img.height, "px"));
    };
  };
  var runBatchProcess = /*#__PURE__*/function () {
    var _ref7 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee3() {
      var zip, currentLayers, offCanvas, offCtx, _loop, i, content, link, _t3;
      return _regenerator().w(function (_context4) {
        while (1) switch (_context4.p = _context4.n) {
          case 0:
            if (isPro) {
              _context4.n = 1;
              break;
            }
            setProModalOpen(true);
            return _context4.a(2);
          case 1:
            setIsBatchProcessing(true);
            setStatusMsg("\uD83D\uDCE6 Batch processing ".concat(fileQueue.length, " files..."));
            _context4.p = 2;
            zip = new JSZip();
            currentLayers = _toConsumableArray(layers); // Use a hidden canvas for background processing
            offCanvas = document.createElement("canvas");
            offCtx = offCanvas.getContext("2d");
            _loop = /*#__PURE__*/_regenerator().m(function _loop() {
              var file, dataUrl, img, blob;
              return _regenerator().w(function (_context3) {
                while (1) switch (_context3.n) {
                  case 0:
                    file = fileQueue[i];
                    setStatusMsg("\uD83D\uDCE6 Processing ".concat(i + 1, "/").concat(fileQueue.length, ": ").concat(file.name));

                    // Load file as Image
                    _context3.n = 1;
                    return new Promise(function (res) {
                      var r = new FileReader();
                      r.onload = function (x) {
                        return res(x.target.result);
                      };
                      r.readAsDataURL(file);
                    });
                  case 1:
                    dataUrl = _context3.v;
                    _context3.n = 2;
                    return new Promise(function (res) {
                      var m = new Image();
                      m.onload = function () {
                        return res(m);
                      };
                      m.src = dataUrl;
                    });
                  case 2:
                    img = _context3.v;
                    offCanvas.width = img.width;
                    offCanvas.height = img.height;
                    offCtx.drawImage(img, 0, 0);

                    // Render current layers on top
                    currentLayers.forEach(function (l) {
                      return drawLayer(offCtx, l);
                    });

                    // Add to zip
                    _context3.n = 3;
                    return new Promise(function (res) {
                      return offCanvas.toBlob(res, "image/png");
                    });
                  case 3:
                    blob = _context3.v;
                    zip.file(file.name.replace(/\.[^/.]+$/, "") + "-bimp.png", blob);
                  case 4:
                    return _context3.a(2);
                }
              }, _loop);
            });
            i = 0;
          case 3:
            if (!(i < fileQueue.length)) {
              _context4.n = 5;
              break;
            }
            return _context4.d(_regeneratorValues(_loop()), 4);
          case 4:
            i++;
            _context4.n = 3;
            break;
          case 5:
            _context4.n = 6;
            return zip.generateAsync({
              type: "blob"
            });
          case 6:
            content = _context4.v;
            link = document.createElement("a");
            link.href = URL.createObjectURL(content);
            link.download = "bimp-batch-".concat(Date.now(), ".zip");
            link.click();
            setStatusMsg("\u2713 Batch complete! ".concat(fileQueue.length, " images exported."));
            _context4.n = 8;
            break;
          case 7:
            _context4.p = 7;
            _t3 = _context4.v;
            console.error(_t3);
            setStatusMsg("Batch Error. Check console.");
          case 8:
            _context4.p = 8;
            setIsBatchProcessing(false);
            return _context4.f(8);
          case 9:
            return _context4.a(2);
        }
      }, _callee3, null, [[2, 7, 8, 9]]);
    }));
    return function runBatchProcess() {
      return _ref7.apply(this, arguments);
    };
  }();
  useEffect(function () {
    var handlePaste = function handlePaste(e) {
      var _e$clipboardData;
      var items = (_e$clipboardData = e.clipboardData) === null || _e$clipboardData === void 0 ? void 0 : _e$clipboardData.items;
      if (!items) return;
      var _iterator = _createForOfIteratorHelper(items),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var item = _step.value;
          if (item.type.startsWith("image/")) {
            var blob = item.getAsFile();
            var reader = new FileReader();
            reader.onload = function (ev) {
              return loadImage(ev.target.result);
            };
            reader.readAsDataURL(blob);
            break;
          }
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    };
    window.addEventListener("paste", handlePaste);
    return function () {
      return window.removeEventListener("paste", handlePaste);
    };
  }, [loadImage]);
  useEffect(function () {
    var handleKey = function handleKey(e) {
      if (e.target.tagName === "INPUT" || e.target.tagName === "TEXTAREA") return;
      if ((e.metaKey || e.ctrlKey) && e.key === "z") {
        e.preventDefault();
        undo();
        return;
      }
      if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "t") {
        e.preventDefault();
        setTool("select");
        status("Select tool active (Ctrl+T)");
        return;
      }
      var t = TOOLS.find(function (t) {
        return t.shortcut === e.key.toUpperCase();
      });
      if (t) setTool(t.id);
    };
    window.addEventListener("keydown", handleKey);
    return function () {
      return window.removeEventListener("keydown", handleKey);
    };
  }, [undo]);
  var getPos = function getPos(e) {
    var canvas = canvasRef.current;
    var rect = canvas.getBoundingClientRect();
    var scaleX = canvas.width / rect.width;
    var scaleY = canvas.height / rect.height;
    var clientX = e.touches ? e.touches[0].clientX : e.clientX;
    var clientY = e.touches ? e.touches[0].clientY : e.clientY;
    return {
      x: (clientX - rect.left) * scaleX,
      y: (clientY - rect.top) * scaleY
    };
  };
  var applyBlur = function applyBlur(ctx, x, y, w, h) {
    var radius = arguments.length > 5 && arguments[5] !== undefined ? arguments[5] : 10;
    var sw = Math.floor(w),
      sh = Math.floor(h);
    if (sw <= 0 || sh <= 0) return;
    ctx.save();
    ctx.filter = "blur(".concat(radius, "px)");
    var d = ctx.getImageData(x, y, sw, sh);
    var o = document.createElement("canvas");
    o.width = sw;
    o.height = sh;
    o.getContext("2d").putImageData(d, 0, 0);
    ctx.drawImage(o, x, y);
    ctx.restore();
  };
  var applyPixelate = function applyPixelate(ctx, x, y, w, h) {
    var blockSize = arguments.length > 5 && arguments[5] !== undefined ? arguments[5] : 12;
    var sw = Math.floor(w),
      sh = Math.floor(h);
    if (sw <= 0 || sh <= 0) return;
    var imageData = ctx.getImageData(x, y, sw, sh);
    var data = imageData.data,
      width = imageData.width,
      height = imageData.height;
    for (var py = 0; py < height; py += blockSize) {
      for (var px = 0; px < width; px += blockSize) {
        var i = (py * width + px) * 4;
        var r = data[i],
          g = data[i + 1],
          b = data[i + 2],
          a = data[i + 3];
        for (var fy = 0; fy < blockSize && py + fy < height; fy++) {
          for (var fx = 0; fx < blockSize && px + fx < width; fx++) {
            var j = ((py + fy) * width + (px + fx)) * 4;
            data[j] = r;
            data[j + 1] = g;
            data[j + 2] = b;
            data[j + 3] = a;
          }
        }
      }
    }
    ctx.putImageData(imageData, x, y);
  };
  var drawArrowOnCanvas = function drawArrowOnCanvas(ctx, x1, y1, x2, y2) {
    var color = arguments.length > 5 && arguments[5] !== undefined ? arguments[5] : "#FF4757";
    var size = arguments.length > 6 && arguments[6] !== undefined ? arguments[6] : 3;
    var style = arguments.length > 7 && arguments[7] !== undefined ? arguments[7] : "classic";
    var dx = x2 - x1,
      dy = y2 - y1;
    var angle = Math.atan2(dy, dx);
    var len = Math.sqrt(dx * dx + dy * dy);
    if (len < 5) return;
    ctx.save();
    ctx.strokeStyle = color;
    ctx.fillStyle = color;
    ctx.lineJoin = "round";
    ctx.lineCap = "round";

    // Use rounded coordinates to prevent "3D" sub-pixel blurring
    var rx1 = Math.round(x1),
      ry1 = Math.round(y1);
    var rx2 = Math.round(x2),
      ry2 = Math.round(y2);
    if (style === "elegant") {
      ctx.lineWidth = Math.max(2, size * 0.8);
      var headLen = 20 + size * 3;
      var headAngle = Math.PI / 10;

      // Shaft all the way to end
      ctx.beginPath();
      ctx.moveTo(rx1, ry1);
      ctx.lineTo(rx2, ry2);
      ctx.stroke();

      // Sharp Pinch-Head (Keep filled but simplified)
      ctx.beginPath();
      ctx.moveTo(rx2, ry2);
      ctx.lineTo(rx2 - headLen * Math.cos(angle - headAngle), ry2 - headLen * Math.sin(angle - headAngle));
      ctx.lineTo(rx2 - headLen * 0.4 * Math.cos(angle), ry2 - headLen * 0.4 * Math.sin(angle));
      ctx.lineTo(rx2 - headLen * Math.cos(angle + headAngle), ry2 - headLen * Math.sin(angle + headAngle));
      ctx.closePath();
      ctx.fill();
    } else if (style === "bold") {
      ctx.lineWidth = size * 2.5 + 2;
      var _headLen = 22 + size * 4;
      ctx.beginPath();
      ctx.moveTo(rx1, ry1);
      ctx.lineTo(rx2, ry2);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(rx2, ry2);
      ctx.lineTo(rx2 - _headLen * Math.cos(angle - Math.PI / 6), ry2 - _headLen * Math.sin(angle - Math.PI / 6));
      ctx.lineTo(rx2 - _headLen * Math.cos(angle + Math.PI / 6), ry2 - _headLen * Math.sin(angle + Math.PI / 6));
      ctx.closePath();
      ctx.fill();
    } else if (style === "dashed") {
      ctx.lineWidth = size + 1;
      var _headLen2 = 16 + size * 2;
      var _headAngle = Math.PI / 7;
      ctx.setLineDash([size * 4 + 4, size * 2 + 2]);
      ctx.beginPath();
      ctx.moveTo(rx1, ry1);
      ctx.lineTo(rx2, ry2);
      ctx.stroke();
      ctx.setLineDash([]);
      // Open "V" Head
      ctx.beginPath();
      ctx.moveTo(rx2 - _headLen2 * Math.cos(angle - _headAngle), ry2 - _headLen2 * Math.sin(angle - _headAngle));
      ctx.lineTo(rx2, ry2);
      ctx.lineTo(rx2 - _headLen2 * Math.cos(angle + _headAngle), ry2 - _headLen2 * Math.sin(angle + _headAngle));
      ctx.stroke();
    } else {
      // Classic "V" Arrow - Ultra Minimal
      ctx.lineWidth = size + 1;
      var _headLen3 = 18 + size * 2.5;
      var _headAngle2 = Math.PI / 7;

      // 1. One clean shaft
      ctx.beginPath();
      ctx.moveTo(rx1, ry1);
      ctx.lineTo(rx2, ry2);
      ctx.stroke();

      // 2. Open "V" head (no fill, no complex overlap)
      ctx.beginPath();
      ctx.moveTo(rx2 - _headLen3 * Math.cos(angle - _headAngle2), ry2 - _headLen3 * Math.sin(angle - _headAngle2));
      ctx.lineTo(rx2, ry2);
      ctx.lineTo(rx2 - _headLen3 * Math.cos(angle + _headAngle2), ry2 - _headLen3 * Math.sin(angle + _headAngle2));
      ctx.stroke();
    }
    ctx.restore();
  };

  // Keep cropSelRef in sync so getCropHandleAtPos always sees current value
  var setCrop = function setCrop(val) {
    cropSelRef.current = val;
    setCropSelection(val);
  };

  // ── Crop handle hit-test — uses ref, never stale ──────────────────────────
  var HANDLE_R = 10; // hit radius in screen px (generous for usability)
  var getCropHandleAtPos = function getCropHandleAtPos(clientX, clientY) {
    var sel = cropSelRef.current;
    if (!sel || !canvasRef.current) return null;
    var canvas = canvasRef.current;
    var rect = canvas.getBoundingClientRect();
    var scaleX = rect.width / canvas.width;
    var scaleY = rect.height / canvas.height;
    var ox = sel.x * scaleX + rect.left;
    var oy = sel.y * scaleY + rect.top;
    var ow = sel.w * scaleX;
    var oh = sel.h * scaleY;
    var handles = {
      nw: [ox, oy],
      n: [ox + ow / 2, oy],
      ne: [ox + ow, oy],
      w: [ox, oy + oh / 2],
      e: [ox + ow, oy + oh / 2],
      sw: [ox, oy + oh],
      s: [ox + ow / 2, oy + oh],
      se: [ox + ow, oy + oh]
    };
    for (var _i = 0, _Object$entries = Object.entries(handles); _i < _Object$entries.length; _i++) {
      var _Object$entries$_i = _slicedToArray(_Object$entries[_i], 2),
        name = _Object$entries$_i[0],
        _Object$entries$_i$ = _slicedToArray(_Object$entries$_i[1], 2),
        hx = _Object$entries$_i$[0],
        hy = _Object$entries$_i$[1];
      if (Math.abs(clientX - hx) <= HANDLE_R && Math.abs(clientY - hy) <= HANDLE_R) return name;
    }
    if (clientX >= ox && clientX <= ox + ow && clientY >= oy && clientY <= oy + oh) return 'move';
    return null;
  };
  var handleMouseDown = function handleMouseDown(e) {
    if (!image) return;
    if (e.target.setPointerCapture) e.target.setPointerCapture(e.pointerId);
    var pos = getPos(e);
    setStartPos(pos);
    setIsDrawing(true);
    if (tool === "select") {
      var handle = getLayerHandleAtPos(pos);
      if (handle) {
        setResizeHandle(handle);
        var l = layers.find(function (x) {
          return x.id === selectedLayerId;
        });
        setLayerStartPos(_objectSpread({}, l));
      } else {
        var hit = findLayerAtPos(pos);
        if (hit) {
          setSelectedLayerId(hit.id);
          setLayerStartPos(_objectSpread({}, hit));
          setResizeHandle(null);
        } else {
          setSelectedLayerId(null);
          setIsDrawing(false);
        }
      }
      return;
    }
    if (tool === "text") {
      setTextPos(pos);
      setShowTextBox(true);
      setIsDrawing(false);
      return;
    }
    if (tool === "arrow") {
      setArrowStart(pos);
      return;
    }
    if (tool === "crop") {
      var _handle = getCropHandleAtPos(e.clientX, e.clientY);
      if (_handle && cropSelRef.current) {
        cropDragRef.current = {
          mode: _handle,
          startX: e.clientX,
          startY: e.clientY,
          origCrop: _objectSpread({}, cropSelRef.current)
        };
      } else {
        setCrop(null);
        cropDragRef.current = null;
      }
      return;
    }
    if (tool === "draw") {
      lastDrawPos.current = pos;
      setPreviewLayer({
        id: "pw",
        type: "draw",
        points: [pos],
        color: drawColor,
        size: brushSize
      });
    }
  };
  var getLayerHandleAtPos = function getLayerHandleAtPos(pos) {
    if (!selectedLayerId || tool !== "select") return null;
    var l = layers.find(function (x) {
      return x.id === selectedLayerId;
    });
    if (!l) return null;
    var H = 10; // hit area
    if (l.type === "arrow") {
      if (Math.abs(pos.x - l.x1) < H && Math.abs(pos.y - l.y1) < H) return "p1";
      if (Math.abs(pos.x - l.x2) < H && Math.abs(pos.y - l.y2) < H) return "p2";
    } else if (l.type !== "draw" && l.type !== "text") {
      var x = l.x,
        y = l.y,
        w = l.w,
        h = l.h;
      if (Math.abs(pos.x - x) < H && Math.abs(pos.y - y) < H) return "nw";
      if (Math.abs(pos.x - (x + w)) < H && Math.abs(pos.y - y) < H) return "ne";
      if (Math.abs(pos.x - x) < H && Math.abs(pos.y - (y + h)) < H) return "sw";
      if (Math.abs(pos.x - (x + w)) < H && Math.abs(pos.y - (y + h)) < H) return "se";
      if (Math.abs(pos.x - (x + w / 2)) < H && Math.abs(pos.y - y) < H) return "n";
      if (Math.abs(pos.x - (x + w / 2)) < H && Math.abs(pos.y - (y + h)) < H) return "s";
      if (Math.abs(pos.x - x) < H && Math.abs(pos.y - (y + h / 2)) < H) return "w";
      if (Math.abs(pos.x - (x + w)) < H && Math.abs(pos.y - (y + h / 2)) < H) return "e";
    }
    return null;
  };
  var findLayerAtPos = function findLayerAtPos(pos) {
    // Search from top to bottom
    for (var i = layers.length - 1; i >= 0; i--) {
      var l = layers[i];
      if (l.type === "redact" || l.type === "blur" || l.type === "pixelate") {
        if (pos.x >= l.x && pos.x <= l.x + l.w && pos.y >= l.y && pos.y <= l.y + l.h) return l;
      } else if (l.type === "text") {
        var ctx = canvasRef.current.getContext("2d");
        ctx.font = "bold ".concat(l.fontSize, "px Inter");
        var m = ctx.measureText(l.text);
        var th = l.fontSize;
        if (pos.x >= l.x && pos.x <= l.x + m.width && pos.y >= l.y - th && pos.y <= l.y) return l;
      } else if (l.type === "arrow") {
        var dist = distToSegment(pos, {
          x: l.x1,
          y: l.y1
        }, {
          x: l.x2,
          y: l.y2
        });
        if (dist < 20) return l;
      } else if (l.type === "image") {
        if (pos.x >= l.x && pos.x <= l.x + l.w && pos.y >= l.y && pos.y <= l.y + l.h) return l;
      }
    }
    return null;
  };
  var distToSegment = function distToSegment(p, v, w) {
    var l2 = Math.pow(v.x - w.x, 2) + Math.pow(v.y - w.y, 2);
    if (l2 === 0) return Math.sqrt(Math.pow(p.x - v.x, 2) + Math.pow(p.y - v.y, 2));
    var t = ((p.x - v.x) * (w.x - v.x) + (p.y - v.y) * (w.y - v.y)) / l2;
    t = Math.max(0, Math.min(1, t));
    return Math.sqrt(Math.pow(p.x - (v.x + t * (w.x - v.x)), 2) + Math.pow(p.y - (v.y + t * (w.y - v.y)), 2));
  };
  var handleMouseMove = function handleMouseMove(e) {
    if (!image) return;
    var pos = getPos(e);

    // Update cursors when NOT drawing
    if (!isDrawing) {
      if (tool === "select") {
        var handle = getLayerHandleAtPos(pos);
        var cursorMap = {
          nw: 'nw-resize',
          n: 'n-resize',
          ne: 'ne-resize',
          w: 'w-resize',
          e: 'e-resize',
          sw: 'sw-resize',
          s: 's-resize',
          se: 'se-resize',
          p1: 'move',
          p2: 'move'
        };
        canvasRef.current.style.cursor = handle ? cursorMap[handle] : findLayerAtPos(pos) ? "move" : "default";
      }
      if (tool === "crop" && cropSelRef.current) {
        var h = getCropHandleAtPos(e.clientX, e.clientY);
        var _cursorMap = {
          nw: 'nw-resize',
          n: 'n-resize',
          ne: 'ne-resize',
          w: 'w-resize',
          e: 'e-resize',
          sw: 'sw-resize',
          s: 's-resize',
          se: 'se-resize',
          move: 'move'
        };
        canvasRef.current.style.cursor = _cursorMap[h] || 'crosshair';
      }
      return;
    }

    // --- DRAWING / DRAGGING LOGIC ---
    if (tool === "select" && selectedLayerId) {
      var dx = pos.x - startPos.x;
      var dy = pos.y - startPos.y;
      setLayers(function (prev) {
        return prev.map(function (l) {
          if (l.id === selectedLayerId) {
            if (resizeHandle) {
              var nl = _objectSpread({}, l);
              if (l.type === "arrow") {
                if (resizeHandle === "p1") {
                  nl.x1 = pos.x;
                  nl.y1 = pos.y;
                }
                if (resizeHandle === "p2") {
                  nl.x2 = pos.x;
                  nl.y2 = pos.y;
                }
              } else {
                var _dx = pos.x - startPos.x;
                var _dy = pos.y - startPos.y;
                if (resizeHandle.includes("w")) {
                  nl.x = layerStartPos.x + _dx;
                  nl.w = Math.max(10, layerStartPos.w - _dx);
                }
                if (resizeHandle.includes("e")) {
                  nl.w = Math.max(10, layerStartPos.w + _dx);
                }
                if (resizeHandle.includes("n")) {
                  nl.y = layerStartPos.y + _dy;
                  nl.h = Math.max(10, layerStartPos.h - _dy);
                }
                if (resizeHandle.includes("s")) {
                  nl.h = Math.max(10, layerStartPos.h + _dy);
                }

                // Aspect Ratio Lock (Shift key)
                if (e.shiftKey && (nl.type === "image" || nl.type === "text")) {
                  var r = layerStartPos.ratio || layerStartPos.w / layerStartPos.h;
                  if (resizeHandle.length === 2) {
                    // corners
                    if (nl.w / nl.h > r) nl.w = nl.h * r;else nl.h = nl.w / r;
                    if (resizeHandle.includes("w")) nl.x = layerStartPos.x + (layerStartPos.w - nl.w);
                    if (resizeHandle.includes("n")) nl.y = layerStartPos.y + (layerStartPos.h - nl.h);
                  }
                }
              }
              return nl;
            }
            // Normal move
            if (l.type === "arrow") {
              return _objectSpread(_objectSpread({}, l), {}, {
                x1: layerStartPos.x1 + dx,
                y1: layerStartPos.y1 + dy,
                x2: layerStartPos.x2 + dx,
                y2: layerStartPos.y2 + dy
              });
            }
            return _objectSpread(_objectSpread({}, l), {}, {
              x: layerStartPos.x + dx,
              y: layerStartPos.y + dy
            });
          }
          return l;
        });
      });
      return;
    }
    if (tool === "draw") {
      setPreviewLayer(function (prev) {
        return _objectSpread(_objectSpread({}, prev), {}, {
          points: [].concat(_toConsumableArray(prev.points), [pos])
        });
      });
      lastDrawPos.current = pos;
      return;
    }
    if (["redact", "blur", "pixelate"].includes(tool)) {
      var x = Math.floor(Math.min(startPos.x, pos.x)),
        y = Math.floor(Math.min(startPos.y, pos.y));
      var w = Math.floor(Math.abs(pos.x - startPos.x)),
        _h = Math.floor(Math.abs(pos.y - startPos.y));
      if (w > 0 && _h > 0) {
        setPreviewLayer({
          id: "pw",
          type: tool,
          x: x,
          y: y,
          w: w,
          h: _h,
          color: redactColor,
          radius: brushSize
        });
      } else {
        setPreviewLayer(null);
      }
    }
    if (tool === "arrow") {
      setPreviewLayer({
        id: "pw",
        type: "arrow",
        x1: startPos.x,
        y1: startPos.y,
        x2: pos.x,
        y2: pos.y,
        color: drawColor,
        size: brushSize / 8 + 2,
        style: arrowStyle
      });
    }
    if (tool === "crop") {
      var canvas = canvasRef.current;
      var rect = canvas.getBoundingClientRect();
      if (cropDragRef.current) {
        var scaleX = canvas.width / rect.width;
        var scaleY = canvas.height / rect.height;
        var _dx2 = (e.clientX - cropDragRef.current.startX) * scaleX;
        var _dy2 = (e.clientY - cropDragRef.current.startY) * scaleY;
        var o = cropDragRef.current.origCrop;
        var mode = cropDragRef.current.mode;
        var MIN = 10;
        var _x2 = o.x,
          _y = o.y,
          _w = o.w,
          _h2 = o.h;
        if (mode === 'move') {
          _x2 = Math.max(0, Math.min(canvas.width - _w, o.x + _dx2));
          _y = Math.max(0, Math.min(canvas.height - _h2, o.y + _dy2));
        } else {
          if (mode.includes('e')) _w = Math.max(MIN, Math.min(canvas.width - o.x, o.w + _dx2));
          if (mode.includes('w')) {
            var nx = Math.max(0, Math.min(o.x + o.w - MIN, o.x + _dx2));
            _x2 = nx;
            _w = o.x + o.w - nx;
          }
          if (mode.includes('s')) _h2 = Math.max(MIN, Math.min(canvas.height - o.y, o.h + _dy2));
          if (mode.includes('n')) {
            var ny = Math.max(0, Math.min(o.y + o.h - MIN, o.y + _dy2));
            _y = ny;
            _h2 = o.y + o.h - ny;
          }
        }
        setCrop({
          x: Math.round(_x2),
          y: Math.round(_y),
          w: Math.round(_w),
          h: Math.round(_h2)
        });
      } else {
        var _x3 = Math.floor(Math.min(startPos.x, pos.x));
        var _y2 = Math.floor(Math.min(startPos.y, pos.y));
        var _w2 = Math.floor(Math.abs(pos.x - startPos.x));
        var _h3 = Math.floor(Math.abs(pos.y - startPos.y));
        if (_w2 > 2 && _h3 > 2) setCrop({
          x: _x3,
          y: _y2,
          w: _w2,
          h: _h3
        });
      }
      return;
    }

    // Overlay for other rect tools
    if (["redact", "blur", "pixelate"].includes(tool) && overlayRef.current) {
      var _canvas = canvasRef.current;
      var _rect = _canvas.getBoundingClientRect();
      var _scaleX = _rect.width / _canvas.width;
      var _scaleY = _rect.height / _canvas.height;
      var _x4 = Math.min(startPos.x, pos.x);
      var _y3 = Math.min(startPos.y, pos.y);
      var _w3 = Math.abs(pos.x - startPos.x);
      var _h4 = Math.abs(pos.y - startPos.y);
      var containerRect = _canvas.parentElement.getBoundingClientRect();
      overlayRef.current.style.left = _x4 * _scaleX + _rect.left - containerRect.left + "px";
      overlayRef.current.style.top = _y3 * _scaleY + _rect.top - containerRect.top + "px";
      overlayRef.current.style.width = _w3 * _scaleX + "px";
      overlayRef.current.style.height = _h4 * _scaleY + "px";
      overlayRef.current.style.display = "block";
    }
  };
  var handleMouseUp = function handleMouseUp(e) {
    if (e.target && e.target.releasePointerCapture && e.pointerId) {
      try {
        e.target.releasePointerCapture(e.pointerId);
      } catch (err) {}
    }
    if (!image) {
      setIsDrawing(false);
      return;
    }
    if (overlayRef.current) overlayRef.current.style.display = "none";
    if (tool === "select") {
      if (isDrawing) saveHistory();
      setIsDrawing(false);
      return;
    }
    if (tool === "crop") {
      cropDragRef.current = null;
      if (cropSelRef.current && cropSelRef.current.w > 10 && cropSelRef.current.h > 10) {
        status("⊡ Drag handles to resize · drag inside to move · Apply to crop", 0);
      } else {
        setCrop(null);
      }
      setIsDrawing(false);
      return;
    }
    if (!isDrawing) {
      setIsDrawing(false);
      return;
    }
    if (previewLayer) {
      saveHistory();
      setLayers(function (prev) {
        return [].concat(_toConsumableArray(prev), [_objectSpread(_objectSpread({}, previewLayer), {}, {
          id: Date.now()
        })]);
      });
      setPreviewLayer(null);
      status("+ Layer added: ".concat(tool.charAt(0).toUpperCase() + tool.slice(1)));
    }
    setIsDrawing(false);
  };
  var commitText = function commitText() {
    if (!textInput.trim() || !textPos) {
      setShowTextBox(false);
      return;
    }
    saveHistory();
    var newLayer = {
      id: Date.now(),
      type: "text",
      x: textPos.x,
      y: textPos.y,
      text: textInput,
      color: drawColor,
      fontSize: fontSize,
      fontFamily: fontFamily
    };
    setLayers(function (prev) {
      return [].concat(_toConsumableArray(prev), [newLayer]);
    });
    setTextInput("");
    setShowTextBox(false);
    setTextPos(null);
    status("T Text added as layer");
  };
  var exportImage = function exportImage() {
    var format = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : "png";
    renderAll(true);
    var canvas = canvasRef.current;
    var link = document.createElement("a");
    link.download = "bimp-export.".concat(format);
    var mimeMap = {
      png: "image/png",
      jpg: "image/jpeg",
      jpeg: "image/jpeg",
      webp: "image/webp",
      bmp: "image/bmp"
    };
    var mime = mimeMap[format] || "image/png";
    var quality = format === "jpg" || format === "jpeg" || format === "webp" ? 0.95 : undefined;
    link.href = canvas.toDataURL(mime, quality);
    link.click();
    renderAll(false);
    status("\u2B07 Saved as ".concat(format.toUpperCase()));
  };
  var copyToClipboard = /*#__PURE__*/function () {
    var _ref8 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee5() {
      var canvas;
      return _regenerator().w(function (_context6) {
        while (1) switch (_context6.n) {
          case 0:
            renderAll(true);
            canvas = canvasRef.current;
            canvas.toBlob(/*#__PURE__*/function () {
              var _ref9 = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee4(blob) {
                var _t4;
                return _regenerator().w(function (_context5) {
                  while (1) switch (_context5.p = _context5.n) {
                    case 0:
                      _context5.p = 0;
                      _context5.n = 1;
                      return navigator.clipboard.write([new ClipboardItem({
                        "image/png": blob
                      })]);
                    case 1:
                      status("📋 Copied to clipboard");
                      renderAll(false);
                      _context5.n = 3;
                      break;
                    case 2:
                      _context5.p = 2;
                      _t4 = _context5.v;
                      status("Export PNG instead — clipboard blocked");
                      renderAll(false);
                    case 3:
                      return _context5.a(2);
                  }
                }, _callee4, null, [[0, 2]]);
              }));
              return function (_x5) {
                return _ref9.apply(this, arguments);
              };
            }());
          case 1:
            return _context6.a(2);
        }
      }, _callee5);
    }));
    return function copyToClipboard() {
      return _ref8.apply(this, arguments);
    };
  }();
  var applyCrop = function applyCrop(e) {
    if (e) {
      e.preventDefault();
      e.stopPropagation();
    }
    var sel = cropSelRef.current;
    if (!sel || sel.w < 2 || sel.h < 2) {
      status("No crop selection");
      return;
    }
    var x = sel.x,
      y = sel.y,
      w = sel.w,
      h = sel.h;
    var canvas = canvasRef.current;
    var cx = Math.max(0, Math.round(x));
    var cy = Math.max(0, Math.round(y));
    var cw = Math.min(Math.round(w), canvas.width - cx);
    var ch = Math.min(Math.round(h), canvas.height - cy);
    if (cw < 2 || ch < 2) {
      status("Crop area too small");
      return;
    }
    saveHistory();

    // 1. Crop Base Image
    var off = document.createElement("canvas");
    off.width = cw;
    off.height = ch;
    var offCtx = off.getContext("2d");
    offCtx.drawImage(baseImage, cx, cy, cw, ch, 0, 0, cw, ch);
    var newSrc = off.toDataURL();
    var newImg = new Image();
    newImg.src = newSrc;
    newImg.onload = function () {
      setBaseImage(newImg);
      setImage(newSrc);

      // 2. Shift all layers
      setLayers(function (prev) {
        return prev.map(function (l) {
          if (l.type === "arrow") return _objectSpread(_objectSpread({}, l), {}, {
            x1: l.x1 - cx,
            y1: l.y1 - cy,
            x2: l.x2 - cx,
            y2: l.y2 - cy
          });
          if (l.type === "draw") return _objectSpread(_objectSpread({}, l), {}, {
            points: l.points.map(function (p) {
              return {
                x: p.x - cx,
                y: p.y - cy
              };
            })
          });
          return _objectSpread(_objectSpread({}, l), {}, {
            x: l.x - cx,
            y: l.y - cy
          });
        });
      });
      canvas.width = cw;
      canvas.height = ch;
      setImageDimensions({
        w: cw,
        h: ch
      });
      setResizeDims({
        w: cw,
        h: ch
      });
      setCrop(null);
      status("⊡ Cropped (Layers Shifted) ✓");
    };
  };
  var handleResizeChange = function handleResizeChange(field, val) {
    var num = parseInt(val) || 0;
    if (field === "w") {
      if (aspectLock && imageDimensions.w > 0) setResizeDims({
        w: val,
        h: Math.round(num / imageDimensions.w * imageDimensions.h)
      });else setResizeDims(function (d) {
        return _objectSpread(_objectSpread({}, d), {}, {
          w: val
        });
      });
    } else {
      if (aspectLock && imageDimensions.h > 0) setResizeDims({
        h: val,
        w: Math.round(num / imageDimensions.h * imageDimensions.w)
      });else setResizeDims(function (d) {
        return _objectSpread(_objectSpread({}, d), {}, {
          h: val
        });
      });
    }
  };
  var applyResize = function applyResize() {
    var w = parseInt(resizeDims.w);
    var h = parseInt(resizeDims.h);
    if (isNaN(w) || isNaN(h) || w < 2 || h < 2) {
      status("Invalid resize dimensions");
      return;
    }
    var sw = w / imageDimensions.w;
    var sh = h / imageDimensions.h;
    saveHistory();

    // 1. Resize Base Image
    var off = document.createElement("canvas");
    off.width = w;
    off.height = h;
    var offCtx = off.getContext("2d");
    offCtx.drawImage(baseImage, 0, 0, w, h);
    var newSrc = off.toDataURL();
    var newImg = new Image();
    newImg.src = newSrc;
    newImg.onload = function () {
      setBaseImage(newImg);
      setImage(newSrc);

      // 2. Scale all layers
      setLayers(function (prev) {
        return prev.map(function (l) {
          if (l.type === "arrow") {
            return _objectSpread(_objectSpread({}, l), {}, {
              x1: l.x1 * sw,
              y1: l.y1 * sh,
              x2: l.x2 * sw,
              y2: l.y2 * sh,
              size: l.size * sw
            });
          }
          if (l.type === "draw") {
            return _objectSpread(_objectSpread({}, l), {}, {
              points: l.points.map(function (p) {
                return {
                  x: p.x * sw,
                  y: p.y * sh
                };
              }),
              size: (l.size || 5) * sw
            });
          }
          if (l.type === "text") {
            return _objectSpread(_objectSpread({}, l), {}, {
              x: l.x * sw,
              y: l.y * sh,
              fontSize: l.fontSize * sw
            });
          }
          // Rect tools (blur, redact, pixelate)
          return _objectSpread(_objectSpread({}, l), {}, {
            x: l.x * sw,
            y: l.y * sh,
            w: l.w * sw,
            h: l.h * sh,
            radius: (l.radius || 10) * sw
          });
        });
      });
      var canvas = canvasRef.current;
      canvas.width = w;
      canvas.height = h;
      setImageDimensions({
        w: w,
        h: h
      });
      status("\u2921 Resized (Layers Scaled) to ".concat(w, "\xD7").concat(h, "px"));
      selectTool("select");
    };
  };
  var cancelCrop = function cancelCrop() {
    setCrop(null);
    status("Crop cancelled");
  };

  // Close social dropdown when clicking outside
  useEffect(function () {
    var handler = function handler(e) {
      if (socialRef.current && !socialRef.current.contains(e.target)) setSocialOpen(false);
      if (saveAsRef.current && !saveAsRef.current.contains(e.target)) setSaveAsOpen(false);
    };
    document.addEventListener("mousedown", handler);
    return function () {
      return document.removeEventListener("mousedown", handler);
    };
  }, []);
  var createSocialCanvas = function createSocialCanvas(preset) {
    var canvas = canvasRef.current;
    var ctx = canvas.getContext("2d");
    // Scale to fit screen
    var maxW = window.innerWidth - 300;
    var maxH = window.innerHeight - 130;
    var displayW = preset.w,
      displayH = preset.h;
    if (displayW > maxW) {
      displayH = displayH * maxW / displayW;
      displayW = maxW;
    }
    if (displayH > maxH) {
      displayW = displayW * maxH / displayH;
      displayH = maxH;
    }
    displayW = Math.round(displayW);
    displayH = Math.round(displayH);
    canvas.width = displayW;
    canvas.height = displayH;
    // Fill with a nice gradient background
    var grad = ctx.createLinearGradient(0, 0, displayW, displayH);
    grad.addColorStop(0, "#1A1A2E");
    grad.addColorStop(1, "#16213E");
    ctx.fillStyle = grad;
    ctx.fillRect(0, 0, displayW, displayH);
    // Subtle grid dots
    ctx.fillStyle = "#FFFFFF08";
    for (var x = 40; x < displayW; x += 40) for (var y = 40; y < displayH; y += 40) {
      ctx.beginPath();
      ctx.arc(x, y, 1, 0, Math.PI * 2);
      ctx.fill();
    }
    // Dashed border guide
    ctx.setLineDash([6, 4]);
    ctx.strokeStyle = "#FFFFFF15";
    ctx.lineWidth = 1;
    ctx.strokeRect(20, 20, displayW - 40, displayH - 40);
    ctx.setLineDash([]);
    // Label in center
    ctx.fillStyle = "#FFFFFF22";
    ctx.font = "bold ".concat(Math.min(18, displayW / 20), "px Inter, sans-serif");
    ctx.textAlign = "center";
    ctx.fillText("".concat(preset.label, " \u2014 ").concat(preset.w, "\xD7").concat(preset.h, "px"), displayW / 2, displayH / 2);
    ctx.textAlign = "left";
    setImageDimensions({
      w: displayW,
      h: displayH
    });
    setResizeDims({
      w: displayW,
      h: displayH
    });
    setIsFit(true);
    setImage("social");
    setShowDropZone(false);
    setHistory([]);
    setSocialOpen(false);
    setActiveSocial(null);
    status("\u2713 ".concat(preset.label, " canvas: ").concat(preset.w, "\xD7").concat(preset.h, "px"), 3000);
  };

  // Clear crop selection when switching away from crop tool
  var selectTool = function selectTool(t) {
    if (t !== "crop" && cropSelRef.current) setCrop(null);
    setTool(t);
  };
  var getCursor = function getCursor() {
    if (tool === "draw") return "crosshair";
    if (tool === "text") return "text";
    if (tool === "select") return "default";
    return "crosshair";
  };
  return React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      height: "100vh",
      background: BRAND.bg,
      color: BRAND.text,
      fontFamily: "'Inter', sans-serif",
      overflow: "hidden"
    }
  },
  // Top bar
  React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      padding: "0 16px",
      height: "52px",
      background: BRAND.surface,
      borderBottom: "1px solid ".concat(BRAND.border),
      flexShrink: 0
    }
  }, React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 10
    }
  }, React.createElement("a", {
    href: "index.html",
    style: {
      display: "flex",
      alignItems: "center",
      gap: 10,
      textDecoration: "none"
    }
  }, React.createElement("img", {
    src: "logo.png",
    alt: "bimp.us",
    style: {
      height: 32,
      width: "auto",
      objectFit: "contain",
      mixBlendMode: "screen"
    }
  })), React.createElement("span", {
    style: {
      fontSize: 10,
      fontWeight: 700,
      color: "#000",
      background: "#FFF",
      borderRadius: 4,
      padding: "2px 6px",
      letterSpacing: 0.5
    }
  }, "BETA")), React.createElement("div", {
    style: {
      fontSize: 13,
      color: statusMsg.includes("✓") || statusMsg.includes("⬇") ? BRAND.success : BRAND.textMuted
    }
  }, statusMsg), React.createElement("div", {
    style: {
      display: "flex",
      gap: 8,
      alignItems: "center"
    }
  },
  // ── Upgrade to Pro ──
  !isPro && React.createElement("button", {
    onClick: function onClick() {
      return setProModalOpen(true);
    },
    style: {
      padding: "6px 12px",
      borderRadius: 7,
      fontSize: 12,
      fontWeight: 700,
      cursor: "pointer",
      border: "1px solid #FFD700",
      background: "#FFD700",
      color: "#000",
      transition: "all 0.15s",
      display: "flex",
      alignItems: "center",
      gap: 6
    }
  }, React.createElement("span", {
    style: {
      fontSize: 14
    }
  }, "💎"), "Upgrade to Pro"),
  // ── Smart Redact ──
  React.createElement("div", {
    onClick: handleAutoRedact,
    style: _objectSpread(_objectSpread({}, btnStyle(BRAND)), {}, {
      border: "1px solid ".concat(BRAND.accent),
      background: isPro ? "rgba(255,215,0,0.1)" : "transparent",
      marginBottom: 12,
      justifyContent: "center",
      position: "relative"
    })
  }, React.createElement("span", {
    style: {
      fontSize: 14
    }
  }, "🤖"), React.createElement("span", {
    style: {
      fontWeight: 700
    }
  }, "Smart Redact"), !isPro && React.createElement("div", {
    style: {
      position: "absolute",
      top: -6,
      right: -6,
      background: "#FFD700",
      color: "#000",
      fontSize: 9,
      padding: "2px 4px",
      borderRadius: 4,
      fontWeight: 900
    }
  }, "PRO")),
  // ── Batch Process ──
  fileQueue.length > 1 && React.createElement("div", {
    onClick: runBatchProcess,
    style: _objectSpread(_objectSpread({}, btnStyle(BRAND)), {}, {
      border: "1px solid ".concat(BRAND.success),
      background: "rgba(46,204,113,0.1)",
      marginBottom: 12,
      justifyContent: "center"
    })
  }, React.createElement("span", {
    style: {
      fontSize: 14
    }
  }, "📦"), React.createElement("span", {
    style: {
      fontWeight: 700,
      color: BRAND.success
    }
  }, "Export All (".concat(fileQueue.length, ")"))),
  // ── Device Mockups ──
  React.createElement("div", {
    onClick: function onClick() {
      return applyMockup("browser");
    },
    style: _objectSpread(_objectSpread({}, btnStyle(BRAND)), {}, {
      border: "1px solid ".concat(BRAND.accent),
      background: isPro ? "rgba(255,215,0,0.1)" : "transparent",
      marginBottom: 12,
      justifyContent: "center",
      position: "relative"
    })
  }, React.createElement("span", {
    style: {
      fontSize: 14
    }
  }, "💻"), React.createElement("span", {
    style: {
      fontWeight: 700
    }
  }, "Device Mockup"), !isPro && React.createElement("div", {
    style: {
      position: "absolute",
      top: -6,
      right: -6,
      background: "#FFD700",
      color: "#000",
      fontSize: 9,
      padding: "2px 4px",
      borderRadius: 4,
      fontWeight: 900
    }
  }, "PRO")),
  // ── Tools list ──
  React.createElement("div", {
    ref: socialRef,
    style: {
      position: "relative"
    }
  }, React.createElement("button", {
    onClick: function onClick() {
      setSocialOpen(function (o) {
        return !o;
      });
      setActiveSocial(null);
    },
    style: {
      padding: "6px 12px",
      borderRadius: 7,
      fontSize: 12,
      fontWeight: 700,
      cursor: "pointer",
      border: "1px solid ".concat(BRAND.border),
      background: socialOpen ? BRAND.accent : "transparent",
      color: socialOpen ? "#000" : BRAND.text,
      transition: "all 0.15s",
      display: "flex",
      alignItems: "center",
      gap: 6
    }
  }, React.createElement("span", {
    style: {
      fontSize: 14
    }
  }, "📐"), "Social Canvas", React.createElement("span", {
    style: {
      fontSize: 10,
      opacity: 0.7
    }
  }, socialOpen ? "▲" : "▼")),
  // Dropdown panel
  socialOpen && React.createElement("div", {
    style: {
      position: "absolute",
      top: "calc(100% + 8px)",
      right: 0,
      background: "#12121F",
      border: "1px solid ".concat(BRAND.border),
      borderRadius: 12,
      boxShadow: "0 16px 48px rgba(0,0,0,0.7)",
      zIndex: 200,
      minWidth: 220,
      overflow: "hidden"
    }
  }, React.createElement("div", {
    style: {
      padding: "10px 14px 6px",
      fontSize: 11,
      color: BRAND.textMuted,
      fontWeight: 700,
      letterSpacing: 1,
      textTransform: "uppercase"
    }
  }, "Choose Platform"), SOCIALS.map(function (platform) {
    return React.createElement(React.Fragment, {
      key: platform.id
    },
    // Platform row
    React.createElement("div", {
      onClick: function onClick() {
        return setActiveSocial(activeSocial === platform.id ? null : platform.id);
      },
      style: {
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        padding: "9px 14px",
        cursor: "pointer",
        background: activeSocial === platform.id ? platform.color + "18" : "transparent",
        borderLeft: activeSocial === platform.id ? "3px solid ".concat(platform.color) : "3px solid transparent",
        transition: "all 0.12s"
      }
    }, React.createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: 10
      }
    }, React.createElement("span", {
      style: {
        color: platform.color,
        display: "flex",
        alignItems: "center"
      }
    }, platform.icon), React.createElement("span", {
      style: {
        fontSize: 13,
        fontWeight: 600,
        color: BRAND.text
      }
    }, platform.label)), React.createElement("span", {
      style: {
        fontSize: 10,
        color: BRAND.textMuted
      }
    }, activeSocial === platform.id ? "▲" : "▶")),
    // Size submenu
    activeSocial === platform.id && React.createElement("div", {
      style: {
        background: "#0A0A14",
        borderTop: "1px solid ".concat(BRAND.border, "33"),
        borderBottom: "1px solid ".concat(BRAND.border, "33")
      }
    }, platform.sizes.map(function (size) {
      return React.createElement("div", {
        key: size.label,
        onClick: function onClick() {
          return createSocialCanvas(_objectSpread(_objectSpread({}, size), {}, {
            label: size.label
          }));
        },
        style: {
          display: "flex",
          alignItems: "center",
          justifyContent: "space-between",
          padding: "8px 14px 8px 36px",
          cursor: "pointer",
          transition: "background 0.1s"
        },
        onMouseEnter: function onMouseEnter(e) {
          return e.currentTarget.style.background = platform.color + "18";
        },
        onMouseLeave: function onMouseLeave(e) {
          return e.currentTarget.style.background = "transparent";
        }
      }, React.createElement("span", {
        style: {
          fontSize: 12,
          color: BRAND.text
        }
      }, size.label), React.createElement("span", {
        style: {
          fontSize: 11,
          color: BRAND.textMuted,
          fontFamily: "monospace"
        }
      }, "".concat(size.w, "\xD7").concat(size.h)));
    })));
  }), React.createElement("div", {
    style: {
      padding: "8px 14px 10px",
      fontSize: 11,
      color: BRAND.textMuted,
      borderTop: "1px solid ".concat(BRAND.border)
    }
  }, "Creates a blank canvas at exact size"))), image && React.createElement(React.Fragment, null, React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 8,
      margin: "0 10px 0 0",
      background: "#12121F",
      padding: "4px 12px",
      borderRadius: 8,
      border: "1px solid ".concat(BRAND.border)
    }
  }, React.createElement("input", {
    type: "range",
    min: 10,
    max: 400,
    value: displayZoom,
    onChange: function onChange(e) {
      setIsFit(false);
      setCustomZoom(parseInt(e.target.value));
    },
    style: {
      width: 90,
      accentColor: BRAND.accent,
      cursor: "pointer"
    }
  }), React.createElement("span", {
    style: {
      fontSize: 12,
      color: BRAND.text,
      fontWeight: 700,
      width: 38,
      cursor: "pointer",
      textAlign: "right",
      userSelect: "none"
    },
    onClick: function onClick() {
      return setIsFit(true);
    },
    title: "Click to auto fit"
  }, "".concat(displayZoom, "%"))), React.createElement("button", {
    onClick: undo,
    disabled: history.length === 0,
    style: btnStyle(BRAND, history.length === 0),
    title: "Undo (Ctrl+Z)"
  }, "↩ Undo"), React.createElement("button", {
    onClick: copyToClipboard,
    style: btnStyle(BRAND)
  }, "📋 Copy"), React.createElement("button", {
    onClick: function onClick() {
      return exportImage("png");
    },
    style: btnStyle(BRAND, false, true)
  }, "⬇ PNG"), React.createElement("button", {
    onClick: function onClick() {
      return exportImage("jpg");
    },
    style: btnStyle(BRAND)
  }, "⬇ JPG"),
  // Save As dropdown
  React.createElement("div", {
    ref: saveAsRef,
    style: {
      position: "relative"
    }
  }, React.createElement("button", {
    onClick: function onClick() {
      return setSaveAsOpen(function (o) {
        return !o;
      });
    },
    style: _objectSpread(_objectSpread({}, btnStyle(BRAND)), {}, {
      gap: 4
    })
  }, "⬇ More ▾"), saveAsOpen && React.createElement("div", {
    style: {
      position: "absolute",
      top: "calc(100% + 6px)",
      right: 0,
      background: "#12121F",
      border: "1px solid ".concat(BRAND.border),
      borderRadius: 10,
      boxShadow: "0 12px 32px rgba(0,0,0,0.6)",
      zIndex: 200,
      minWidth: 160,
      overflow: "hidden"
    }
  }, React.createElement("div", {
    style: {
      padding: "8px 12px 4px",
      fontSize: 10,
      color: BRAND.textMuted,
      fontWeight: 700,
      letterSpacing: 1,
      textTransform: "uppercase"
    }
  }, "Save As"), [{
    fmt: "webp",
    label: "WebP",
    note: "Best for web — smallest size"
  }, {
    fmt: "bmp",
    label: "BMP",
    note: "Uncompressed bitmap"
  }].map(function (_ref0) {
    var fmt = _ref0.fmt,
      label = _ref0.label,
      note = _ref0.note;
    return React.createElement("div", {
      key: fmt,
      onClick: function onClick() {
        exportImage(fmt);
        setSaveAsOpen(false);
      },
      style: {
        padding: "9px 14px",
        cursor: "pointer",
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        gap: 12
      },
      onMouseEnter: function onMouseEnter(e) {
        return e.currentTarget.style.background = BRAND.surfaceHover;
      },
      onMouseLeave: function onMouseLeave(e) {
        return e.currentTarget.style.background = "transparent";
      }
    }, React.createElement("span", {
      style: {
        fontSize: 13,
        fontWeight: 600,
        color: BRAND.text
      }
    }, "\u2B07 ".concat(label)), React.createElement("span", {
      style: {
        fontSize: 10,
        color: BRAND.textMuted
      }
    }, note));
  })))), React.createElement("button", {
    onClick: function onClick() {
      return fileInputRef.current.click();
    },
    style: btnStyle(BRAND)
  }, "📁 Open"), React.createElement("input", {
    ref: fileInputRef,
    type: "file",
    accept: "image/*",
    style: {
      display: "none"
    },
    onChange: handleFileInput
  }))),
  // Body
  React.createElement("div", {
    style: {
      display: "flex",
      flex: 1,
      overflow: "hidden"
    }
  },
  // Left toolbar — wider to show labels
  React.createElement("div", {
    style: {
      width: 76,
      background: BRAND.surface,
      borderRight: "1px solid ".concat(BRAND.border),
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      padding: "10px 0",
      gap: 2,
      flexShrink: 0,
      overflowY: "auto"
    }
  }, TOOLS.map(function (t) {
    return React.createElement("button", {
      key: t.id,
      onClick: function onClick() {
        return selectTool(t.id);
      },
      title: "".concat(t.label, " (").concat(t.shortcut, ")"),
      "aria-label": t.label,
      style: {
        width: 64,
        borderRadius: 9,
        background: tool === t.id ? toolColors[t.id] + "22" : "transparent",
        border: tool === t.id ? "1.5px solid ".concat(toolColors[t.id]) : "1.5px solid transparent",
        color: tool === t.id ? toolColors[t.id] : BRAND.textMuted,
        cursor: "pointer",
        transition: "all 0.15s",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        padding: "6px 0",
        gap: 2
      }
    }, React.createElement("span", {
      style: {
        fontSize: t.id === "text" ? 15 : 18,
        fontWeight: t.id === "text" ? 900 : 400,
        lineHeight: 1
      }
    }, t.icon), React.createElement("span", {
      style: {
        fontSize: 9,
        fontWeight: 600,
        letterSpacing: 0.2,
        opacity: tool === t.id ? 1 : 0.7
      }
    }, t.label));
  }), React.createElement("div", {
    style: {
      flex: 1
    }
  }),
  // Divider
  React.createElement("div", {
    style: {
      width: 44,
      height: 1,
      background: BRAND.border,
      margin: "6px 0"
    }
  }),
  // Arrow styles (only active if tool is arrow)
  tool === "arrow" && React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: 4,
      paddingBottom: 6,
      width: "100%"
    }
  }, React.createElement("span", {
    style: {
      fontSize: 8,
      color: BRAND.textMuted,
      fontWeight: 600,
      letterSpacing: 0.5,
      textTransform: "uppercase"
    }
  }, "Style"), React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "1fr 1fr",
      gap: 3,
      width: "100%",
      padding: "0 6px"
    }
  }, [{
    id: "classic",
    icon: "↗"
  }, {
    id: "elegant",
    icon: "⤤"
  }, {
    id: "dashed",
    icon: "⤏"
  }, {
    id: "bold",
    icon: "➔"
  }].map(function (s) {
    return React.createElement("div", {
      key: s.id,
      onClick: function onClick() {
        return setArrowStyle(s.id);
      },
      title: s.id.charAt(0).toUpperCase() + s.id.slice(1),
      style: {
        height: 26,
        borderRadius: 6,
        background: arrowStyle === s.id ? BRAND.accent + "33" : "transparent",
        border: arrowStyle === s.id ? "1px solid ".concat(BRAND.accent) : "1px solid ".concat(BRAND.border),
        color: arrowStyle === s.id ? BRAND.accent : BRAND.textMuted,
        cursor: "pointer",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: 13,
        transition: "all 0.1s"
      }
    }, s.icon);
  }))), tool === "arrow" && React.createElement("div", {
    style: {
      width: 44,
      height: 1,
      background: BRAND.border,
      margin: "0 0 6px 0"
    }
  }),
  // Color presets
  React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: 4,
      paddingBottom: 6
    }
  }, React.createElement("span", {
    style: {
      fontSize: 8,
      color: BRAND.textMuted,
      fontWeight: 600,
      letterSpacing: 0.5,
      textTransform: "uppercase"
    }
  }, "Color"), React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "1fr 1fr 1fr",
      gap: 3
    }
  }, COLOR_PRESETS.map(function (c) {
    return React.createElement("div", {
      key: c,
      onClick: function onClick() {
        return setDrawColor(c);
      },
      style: {
        width: 16,
        height: 16,
        borderRadius: 4,
        background: c,
        border: drawColor === c ? "2px solid white" : c === "#FFFFFF" ? "1px solid ".concat(BRAND.border) : "2px solid transparent",
        cursor: "pointer",
        transition: "transform 0.1s",
        transform: drawColor === c ? "scale(1.25)" : "scale(1)"
      }
    });
  })),
  // Custom color picker dot
  React.createElement("div", {
    style: {
      width: 28,
      height: 28,
      borderRadius: "50%",
      background: "conic-gradient(red, yellow, lime, cyan, blue, magenta, red)",
      border: "2px solid ".concat(BRAND.border),
      cursor: "pointer",
      position: "relative",
      overflow: "hidden",
      marginTop: 2
    },
    title: "Custom color"
  }, React.createElement("input", {
    type: "color",
    value: drawColor,
    onChange: function onChange(e) {
      return setDrawColor(e.target.value);
    },
    style: {
      position: "absolute",
      inset: 0,
      opacity: 0,
      cursor: "pointer",
      width: "100%",
      height: "100%"
    }
  }))),
  // ── Carbon Ads Placement ──
  !isPro && React.createElement("div", {
    id: "carbon-ads-container",
    style: {
      width: "100%",
      marginTop: "auto",
      padding: "10px 0",
      borderTop: "1px solid ".concat(BRAND.border)
    }
  }, React.createElement("div", {
    id: "carbonads"
  }, React.createElement("span", {
    style: {
      fontSize: 10,
      color: BRAND.textMuted,
      opacity: 0.5
    }
  }, "Ad support bimp.us")))),
  // Canvas area — canvas is ALWAYS in the DOM so canvasRef is always valid
  React.createElement("div", {
    style: {
      flex: 1,
      overflow: "auto",
      position: "relative",
      background: showDropZone ? BRAND.bg : "repeating-conic-gradient(#1A1A2E 0% 25%, #0F0F1A 0% 50%) 0 0 / 20px 20px"
    },
    onDrop: handleDrop,
    onDragOver: function onDragOver(e) {
      return e.preventDefault();
    }
  },
  // Drop zone overlay — sits on top when no image loaded
  showDropZone && React.createElement("div", {
    style: {
      position: "absolute",
      inset: 0,
      zIndex: 10,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      background: BRAND.bg,
      cursor: "pointer"
    },
    onClick: function onClick() {
      return fileInputRef.current.click();
    }
  }, React.createElement("div", {
    style: {
      textAlign: "center",
      padding: 48,
      border: "2px dashed ".concat(BRAND.border),
      borderRadius: 20,
      maxWidth: 420
    }
  }, React.createElement("div", {
    style: {
      fontSize: 52,
      marginBottom: 16
    }
  }, "🖼"), React.createElement("div", {
    style: {
      fontSize: 20,
      fontWeight: 700,
      marginBottom: 8
    }
  }, "Drop image here"), React.createElement("div", {
    style: {
      color: BRAND.textMuted,
      fontSize: 14,
      lineHeight: 1.7
    }
  }, "Press ", React.createElement("kbd", {
    style: {
      background: "#2E2E4A",
      border: "1px solid #4A4A6A",
      borderRadius: 4,
      padding: "1px 6px",
      fontSize: 12
    }
  }, "Ctrl+V"), " to paste from clipboard", React.createElement("br"), "or click to open a file"), React.createElement("div", {
    style: {
      marginTop: 20,
      display: "flex",
      gap: 6,
      justifyContent: "center"
    }
  }, ["PNG", "JPG", "WebP", "GIF"].map(function (f) {
    return React.createElement("span", {
      key: f,
      style: {
        fontSize: 11,
        color: BRAND.textMuted,
        background: BRAND.surface,
        padding: "3px 8px",
        borderRadius: 4,
        border: "1px solid ".concat(BRAND.border)
      }
    }, f);
  })))),
  // Canvas wrapper — always rendered, hidden until image loaded
  React.createElement("div", {
    style: {
      display: showDropZone ? "none" : "inline-block",
      position: "relative",
      padding: 24
    }
  }, React.createElement("canvas", {
    ref: canvasRef,
    style: {
      display: "block",
      cursor: getCursor(),
      boxShadow: "0 8px 40px rgba(0,0,0,0.6)",
      borderRadius: 4,
      touchAction: "none",
      transformOrigin: "top left",
      maxWidth: isFit ? "100%" : "none",
      maxHeight: isFit ? "calc(100vh - 120px)" : "none",
      width: isFit ? "auto" : "".concat(imageDimensions.w * (customZoom / 100), "px"),
      height: isFit ? "auto" : "".concat(imageDimensions.h * (customZoom / 100), "px"),
      objectFit: "contain",
      imageRendering: "auto"
    },
    onPointerDown: handleMouseDown,
    onPointerMove: handleMouseMove,
    onPointerUp: handleMouseUp
  }),
  // Live drag selection overlay
  React.createElement("div", {
    ref: overlayRef,
    style: {
      display: "none",
      position: "absolute",
      pointerEvents: "none",
      border: "2px dashed ".concat(toolColors[tool] || BRAND.accent),
      background: (toolColors[tool] || BRAND.accent) + "20",
      borderRadius: 2,
      zIndex: 5
    }
  }),
  // Persistent crop selection overlay with Apply/Cancel
  cropSelection && canvasRef.current && function (_React2) {
    var canvas = canvasRef.current;
    var rect = canvas.getBoundingClientRect();
    var scaleX = rect.width / canvas.width;
    var scaleY = rect.height / canvas.height;
    var ox = cropSelection.x * scaleX + 24;
    var oy = cropSelection.y * scaleY + 24;
    var ow = cropSelection.w * scaleX;
    var oh = cropSelection.h * scaleY;
    var HSIZE = 8; // handle square half-size in px
    var handlePos = [{
      key: 'nw',
      left: ox - HSIZE,
      top: oy - HSIZE,
      cursor: 'nw-resize'
    }, {
      key: 'n',
      left: ox + ow / 2 - HSIZE,
      top: oy - HSIZE,
      cursor: 'n-resize'
    }, {
      key: 'ne',
      left: ox + ow - HSIZE,
      top: oy - HSIZE,
      cursor: 'ne-resize'
    }, {
      key: 'w',
      left: ox - HSIZE,
      top: oy + oh / 2 - HSIZE,
      cursor: 'w-resize'
    }, {
      key: 'e',
      left: ox + ow - HSIZE,
      top: oy + oh / 2 - HSIZE,
      cursor: 'e-resize'
    }, {
      key: 'sw',
      left: ox - HSIZE,
      top: oy + oh - HSIZE,
      cursor: 'sw-resize'
    }, {
      key: 's',
      left: ox + ow / 2 - HSIZE,
      top: oy + oh - HSIZE,
      cursor: 's-resize'
    }, {
      key: 'se',
      left: ox + ow - HSIZE,
      top: oy + oh - HSIZE,
      cursor: 'se-resize'
    }];
    return (_React2 = React).createElement.apply(_React2, [React.Fragment, null,
    // Dark outside overlay
    React.createElement("div", {
      style: {
        position: "absolute",
        inset: 0,
        background: "rgba(0,0,0,0.45)",
        pointerEvents: "none",
        zIndex: 6
      }
    }),
    // Crop window (clears darkness via box-shadow trick)
    React.createElement("div", {
      style: {
        position: "absolute",
        left: ox,
        top: oy,
        width: ow,
        height: oh,
        boxShadow: "0 0 0 9999px rgba(0,0,0,0.45)",
        border: "2px solid #2ECC71",
        zIndex: 7,
        pointerEvents: "none"
      }
    },
    // Rule-of-thirds grid lines
    React.createElement("div", {
      style: {
        position: "absolute",
        left: "33.3%",
        top: 0,
        width: 1,
        height: "100%",
        background: "rgba(255,255,255,0.2)",
        pointerEvents: "none"
      }
    }), React.createElement("div", {
      style: {
        position: "absolute",
        left: "66.6%",
        top: 0,
        width: 1,
        height: "100%",
        background: "rgba(255,255,255,0.2)",
        pointerEvents: "none"
      }
    }), React.createElement("div", {
      style: {
        position: "absolute",
        left: 0,
        top: "33.3%",
        width: "100%",
        height: 1,
        background: "rgba(255,255,255,0.2)",
        pointerEvents: "none"
      }
    }), React.createElement("div", {
      style: {
        position: "absolute",
        left: 0,
        top: "66.6%",
        width: "100%",
        height: 1,
        background: "rgba(255,255,255,0.2)",
        pointerEvents: "none"
      }
    }))].concat(_toConsumableArray(handlePos.map(function (h) {
      return React.createElement("div", {
        key: h.key,
        style: {
          position: "absolute",
          left: h.left,
          top: h.top,
          width: HSIZE * 2,
          height: HSIZE * 2,
          background: "#fff",
          border: "1.5px solid #2ECC71",
          borderRadius: 2,
          zIndex: 9,
          pointerEvents: "none",
          cursor: h.cursor,
          boxShadow: "0 1px 4px rgba(0,0,0,0.4)"
        }
      });
    })), [
    // Size readout
    React.createElement("div", {
      style: {
        position: "absolute",
        left: ox,
        top: oy - 26,
        background: "rgba(0,0,0,0.75)",
        color: "#2ECC71",
        fontSize: 11,
        fontWeight: 700,
        padding: "2px 8px",
        borderRadius: 4,
        zIndex: 9,
        pointerEvents: "none",
        whiteSpace: "nowrap"
      }
    }, "".concat(cropSelection.w, " \xD7 ").concat(cropSelection.h, "px")),
    // Apply / Cancel buttons — stopPropagation so pointer down doesn't reach canvas handler and clear the selection
    React.createElement("div", {
      onPointerDown: function onPointerDown(e) {
        return e.stopPropagation();
      },
      style: {
        position: "absolute",
        left: ox,
        top: oy + oh + 10,
        display: "flex",
        gap: 6,
        zIndex: 9
      }
    }, React.createElement("button", {
      onPointerDown: function onPointerDown(e) {
        e.stopPropagation();
        applyCrop(e);
      },
      style: {
        padding: "6px 14px",
        borderRadius: 6,
        background: "#2ECC71",
        color: "#000",
        border: "none",
        fontWeight: 700,
        fontSize: 12,
        cursor: "pointer"
      }
    }, "✓ Apply Crop"), React.createElement("button", {
      onPointerDown: function onPointerDown(e) {
        e.stopPropagation();
        cancelCrop();
      },
      style: {
        padding: "6px 10px",
        borderRadius: 6,
        background: "rgba(0,0,0,0.7)",
        color: "#fff",
        border: "1px solid #555",
        fontWeight: 600,
        fontSize: 12,
        cursor: "pointer"
      }
    }, "✕ Cancel"))]));
  }(), showTextBox && textPos && React.createElement("div", {
    style: {
      position: "absolute",
      left: 24 + textPos.x / (canvasRef.current ? canvasRef.current.width / canvasRef.current.getBoundingClientRect().width : 1),
      top: 24 + textPos.y / (canvasRef.current ? canvasRef.current.height / canvasRef.current.getBoundingClientRect().height : 1),
      zIndex: 20
    }
  }, React.createElement("input", {
    autoFocus: true,
    value: textInput,
    onChange: function onChange(e) {
      return setTextInput(e.target.value);
    },
    onKeyDown: function onKeyDown(e) {
      if (e.key === "Enter") commitText();
      if (e.key === "Escape") setShowTextBox(false);
    },
    placeholder: "Type → Enter...",
    style: {
      background: "rgba(0,0,0,0.9)",
      color: drawColor,
      border: "1.5px solid ".concat(drawColor),
      borderRadius: 4,
      padding: "4px 10px",
      fontSize: Math.max(12, fontSize * (canvasRef.current ? canvasRef.current.getBoundingClientRect().width / canvasRef.current.width : 1)),
      fontWeight: 700,
      outline: "none",
      minWidth: 160,
      fontFamily: fontFamily
    }
  })))),
  // Right panel
  image && React.createElement("div", {
    style: {
      width: 200,
      background: BRAND.surface,
      borderLeft: "1px solid ".concat(BRAND.border),
      padding: "16px 14px",
      display: "flex",
      flexDirection: "column",
      gap: 18,
      flexShrink: 0,
      overflowY: "auto"
    }
  }, React.createElement("div", null, React.createElement("div", {
    style: {
      fontSize: 11,
      color: BRAND.textMuted,
      letterSpacing: 1,
      textTransform: "uppercase",
      marginBottom: 6
    }
  }, selectedLayerId ? "Editing Layer" : "Active Tool"), React.createElement("div", {
    style: {
      fontSize: 14,
      fontWeight: 700,
      color: selectedLayerId ? BRAND.accent : toolColors[tool] || BRAND.accent
    }
  }, selectedLayerId ? ((_layers$find = layers.find(function (l) {
    return l.id === selectedLayerId;
  })) === null || _layers$find === void 0 ? void 0 : _layers$find.type.toUpperCase().charAt(0)) + ((_layers$find2 = layers.find(function (l) {
    return l.id === selectedLayerId;
  })) === null || _layers$find2 === void 0 ? void 0 : _layers$find2.type.slice(1)) : (_TOOLS$find = TOOLS.find(function (t) {
    return t.id === tool;
  })) === null || _TOOLS$find === void 0 ? void 0 : _TOOLS$find.label), React.createElement("div", {
    style: {
      fontSize: 11,
      color: BRAND.textMuted,
      marginTop: 4
    }
  }, selectedLayerId ? "Scale & property adjustments active" : {
    select: "Click to inspect",
    redact: "Drag to cover text/info",
    blur: "Drag to blur a region",
    pixelate: "Drag to pixelate a region",
    crop: "Drag to select, then Apply",
    arrow: "Drag to draw arrow",
    text: "Click to place text",
    draw: "Drag to draw freehand"
  }[tool] || "")),
  // Crop — Apply / Cancel
  tool === "crop" && cropSelection && React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 8
    }
  }, React.createElement("div", {
    style: {
      fontSize: 11,
      color: BRAND.textMuted,
      letterSpacing: 1,
      textTransform: "uppercase",
      marginBottom: 4
    }
  }, "Crop Selection"), React.createElement("div", {
    style: {
      fontSize: 12,
      color: BRAND.textMuted
    }
  }, "".concat(cropSelection.w, " \xD7 ").concat(cropSelection.h, "px")), React.createElement("button", {
    onMouseDown: function onMouseDown(e) {
      e.stopPropagation();
      applyCrop(e);
    },
    style: {
      padding: "8px",
      borderRadius: 7,
      background: "#2ECC71",
      color: "#000",
      border: "none",
      fontWeight: 700,
      fontSize: 13,
      cursor: "pointer"
    }
  }, "✓ Apply Crop"), React.createElement("button", {
    onMouseDown: function onMouseDown(e) {
      e.stopPropagation();
      cancelCrop();
    },
    style: {
      padding: "7px",
      borderRadius: 7,
      background: "transparent",
      color: BRAND.textMuted,
      border: "1px solid ".concat(BRAND.border),
      fontWeight: 600,
      fontSize: 12,
      cursor: "pointer"
    }
  }, "✕ Cancel")),
  // Resize — Apply
  tool === "resize" && React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 12,
      marginTop: 8
    }
  }, React.createElement("div", {
    style: {
      fontSize: 11,
      color: BRAND.textMuted,
      letterSpacing: 1,
      textTransform: "uppercase",
      marginBottom: 4
    }
  }, "Resize Dimensions"),
  // Width
  React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between"
    }
  }, React.createElement("span", {
    style: {
      fontSize: 13,
      color: BRAND.textMuted,
      fontWeight: 600
    }
  }, "W"), React.createElement("div", {
    style: {
      position: "relative"
    }
  }, React.createElement("input", {
    type: "number",
    value: resizeDims.w,
    onChange: function onChange(e) {
      return handleResizeChange("w", e.target.value);
    },
    style: {
      width: 80,
      background: BRAND.bg,
      border: "1px solid ".concat(BRAND.border),
      color: "#fff",
      fontSize: 14,
      padding: "6px 8px",
      borderRadius: 6,
      textAlign: "right",
      fontFamily: "monospace",
      outline: "none"
    }
  }), React.createElement("span", {
    style: {
      position: "absolute",
      right: 8,
      top: 7,
      fontSize: 10,
      color: BRAND.textMuted,
      pointerEvents: "none"
    }
  }, "px"))),
  // Height
  React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between"
    }
  }, React.createElement("span", {
    style: {
      fontSize: 13,
      color: BRAND.textMuted,
      fontWeight: 600
    }
  }, "H"), React.createElement("div", {
    style: {
      position: "relative"
    }
  }, React.createElement("input", {
    type: "number",
    value: resizeDims.h,
    onChange: function onChange(e) {
      return handleResizeChange("h", e.target.value);
    },
    style: {
      width: 80,
      background: BRAND.bg,
      border: "1px solid ".concat(BRAND.border),
      color: "#fff",
      fontSize: 14,
      padding: "6px 8px",
      borderRadius: 6,
      textAlign: "right",
      fontFamily: "monospace",
      outline: "none"
    }
  }), React.createElement("span", {
    style: {
      position: "absolute",
      right: 8,
      top: 7,
      fontSize: 10,
      color: BRAND.textMuted,
      pointerEvents: "none"
    }
  }, "px"))),
  // Lock Aspect Ratio
  React.createElement("label", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 8,
      marginTop: 4,
      cursor: "pointer",
      userSelect: "none"
    }
  }, React.createElement("input", {
    type: "checkbox",
    checked: aspectLock,
    onChange: function onChange(e) {
      return setAspectLock(e.target.checked);
    },
    style: {
      accentColor: BRAND.accent,
      cursor: "pointer",
      width: 14,
      height: 14
    }
  }), React.createElement("span", {
    style: {
      fontSize: 12,
      color: BRAND.text
    }
  }, "Lock Aspect Ratio")),
  // Apply Button
  React.createElement("div", {
    style: {
      marginTop: 12,
      display: "flex",
      flexDirection: "column",
      gap: 6
    }
  }, React.createElement("button", {
    onClick: applyResize,
    style: {
      width: "100%",
      padding: "10px 0",
      background: BRAND.accent,
      border: "none",
      color: "#fff",
      borderRadius: 6,
      fontSize: 13,
      fontWeight: 700,
      cursor: "pointer",
      transition: "0.15s"
    },
    onMouseEnter: function onMouseEnter(e) {
      return e.currentTarget.style.background = BRAND.accentHover;
    },
    onMouseLeave: function onMouseLeave(e) {
      return e.currentTarget.style.background = BRAND.accent;
    }
  }, "Apply Resize"), React.createElement("div", {
    style: {
      fontSize: 10,
      color: BRAND.textMuted,
      textAlign: "center"
    }
  }, "Press ", React.createElement("b", {
    style: {
      color: BRAND.text
    }
  }, "Enter"), " to apply or ", React.createElement("b", {
    style: {
      color: BRAND.text
    }
  }, "Esc"), " to cancel"))), function () {
    var sel = layers.find(function (l) {
      return l.id === selectedLayerId;
    });
    var type = sel ? sel.type : tool;
    var elements = [];
    if (["blur", "pixelate", "draw", "arrow"].includes(type)) {
      elements.push(React.createElement("div", {
        key: "brush-size"
      }, React.createElement("div", {
        style: {
          fontSize: 11,
          color: BRAND.textMuted,
          letterSpacing: 1,
          textTransform: "uppercase",
          marginBottom: 8
        }
      }, "".concat(type === "blur" ? "Blur Radius" : type === "pixelate" ? "Block Size" : "Size", " \u2014 ").concat(brushSize)), React.createElement("input", {
        type: "range",
        min: type === "pixelate" ? 4 : type === "blur" ? 4 : 2,
        max: 40,
        value: brushSize,
        onChange: function onChange(e) {
          var v = Number(e.target.value);
          setBrushSize(v);
          updateSelectedLayer({
            radius: v,
            size: v
          });
        },
        style: {
          width: "100%",
          accentColor: BRAND.accent
        }
      })));
    }
    if (type === "text") {
      elements.push(React.createElement("div", {
        key: "font-settings",
        style: {
          display: "flex",
          flexDirection: "column",
          gap: 12
        }
      }, React.createElement("div", null, React.createElement("div", {
        style: {
          fontSize: 11,
          color: BRAND.textMuted,
          letterSpacing: 1,
          textTransform: "uppercase",
          marginBottom: 8
        }
      }, "Font Size \u2014 ".concat(fontSize, "px")), React.createElement("input", {
        type: "range",
        min: 12,
        max: 96,
        value: fontSize,
        onChange: function onChange(e) {
          var v = Number(e.target.value);
          setFontSize(v);
          updateSelectedLayer({
            fontSize: v
          });
        },
        style: {
          width: "100%",
          accentColor: BRAND.accent
        }
      })), React.createElement("div", null, React.createElement("div", {
        style: {
          fontSize: 11,
          color: BRAND.textMuted,
          letterSpacing: 1,
          textTransform: "uppercase",
          marginBottom: 8
        }
      }, "Typeface"), React.createElement("div", {
        style: {
          display: "grid",
          gridTemplateColumns: "1fr 1fr",
          gap: 6
        }
      }, VIGNELLI_FONTS.map(function (f) {
        return React.createElement("div", {
          key: f.name,
          onClick: function onClick() {
            setFontFamily(f.family);
            updateSelectedLayer({
              fontFamily: f.family
            });
          },
          style: {
            padding: "8px 6px",
            borderRadius: 6,
            background: fontFamily === f.family ? BRAND.accent + "33" : BRAND.surfaceHover,
            border: "1px solid ".concat(fontFamily === f.family ? BRAND.accent : BRAND.border),
            color: fontFamily === f.family ? BRAND.accent : BRAND.text,
            cursor: "pointer",
            fontSize: 12,
            textAlign: "center",
            fontFamily: f.family,
            transition: "all 0.1s"
          }
        }, f.name);
      })))));
    }
    if (type === "redact") {
      elements.push(React.createElement("div", {
        key: "redact-color"
      }, React.createElement("div", {
        style: {
          fontSize: 11,
          color: BRAND.textMuted,
          letterSpacing: 1,
          textTransform: "uppercase",
          marginBottom: 8
        }
      }, "Redact Color"), React.createElement("div", {
        style: {
          display: "flex",
          gap: 6,
          flexWrap: "wrap"
        }
      }, REDACT_COLORS.map(function (c) {
        return React.createElement("div", {
          key: c,
          onClick: function onClick() {
            setRedactColor(c);
            updateSelectedLayer({
              color: c
            });
          },
          style: {
            width: 28,
            height: 28,
            borderRadius: 6,
            background: c,
            border: redactColor === c ? "2px solid ".concat(BRAND.accent) : c === "#FFFFFF" ? "1px solid ".concat(BRAND.border) : "1px solid transparent",
            cursor: "pointer"
          }
        });
      }))));
    }
    if (["draw", "text", "arrow"].includes(type)) {
      elements.push(React.createElement("div", {
        key: "general-color"
      }, React.createElement("div", {
        style: {
          fontSize: 11,
          color: BRAND.textMuted,
          letterSpacing: 1,
          textTransform: "uppercase",
          marginBottom: 8
        }
      }, "Color"), React.createElement("div", {
        style: {
          display: "flex",
          gap: 5,
          flexWrap: "wrap",
          marginBottom: 8
        }
      }, COLOR_PRESETS.map(function (c) {
        return React.createElement("div", {
          key: c,
          onClick: function onClick() {
            setDrawColor(c);
            updateSelectedLayer({
              color: c
            });
          },
          style: {
            width: 22,
            height: 22,
            borderRadius: 5,
            background: c,
            border: drawColor === c ? "2px solid white" : c === "#FFFFFF" ? "1px solid ".concat(BRAND.border) : "1px solid transparent",
            cursor: "pointer",
            transition: "transform 0.1s",
            transform: drawColor === c ? "scale(1.2)" : "scale(1)"
          }
        });
      })), React.createElement("input", {
        type: "color",
        value: drawColor,
        onChange: function onChange(e) {
          setDrawColor(e.target.value);
          updateSelectedLayer({
            color: e.target.value
          });
        },
        style: {
          width: "100%",
          height: 28,
          borderRadius: 6,
          border: "1px solid ".concat(BRAND.border),
          cursor: "pointer"
        }
      })));
    }
    return elements;
  }(), React.createElement("div", {
    style: {
      marginTop: "auto",
      paddingTop: 16,
      borderTop: "1px solid ".concat(BRAND.border)
    }
  }, React.createElement("div", {
    style: {
      fontSize: 11,
      color: BRAND.textMuted,
      letterSpacing: 1,
      textTransform: "uppercase",
      marginBottom: 8
    }
  }, "Canvas"), React.createElement("div", {
    style: {
      fontSize: 12,
      color: BRAND.textMuted
    }
  }, "".concat(imageDimensions.w, " \xD7 ").concat(imageDimensions.h, "px")), React.createElement("div", {
    style: {
      fontSize: 12,
      color: BRAND.textMuted,
      marginTop: 4
    }
  }, "".concat(history.length, " edit").concat(history.length !== 1 ? "s" : ""))),
  // LAYER LIST (Photopea style)
  layers.length > 0 && React.createElement("div", {
    style: {
      borderTop: "1px solid ".concat(BRAND.border),
      paddingTop: 16
    }
  }, React.createElement("div", {
    style: {
      fontSize: 11,
      fontWeight: 700,
      color: BRAND.textMuted,
      textTransform: "uppercase",
      letterSpacing: 1,
      marginBottom: 12,
      display: "flex",
      justifyContent: "space-between"
    }
  }, "Layers", React.createElement("span", {
    style: {
      color: BRAND.accent
    }
  }, layers.length)), React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 4,
      maxHeight: 200,
      overflowY: "auto",
      paddingRight: 4
    }
  }, layers.map(function (l, idx) {
    return React.createElement("div", {
      key: l.id,
      onClick: function onClick() {
        setSelectedLayerId(l.id);
        setTool("select");
      },
      style: {
        padding: "8px 10px",
        borderRadius: 6,
        cursor: "pointer",
        fontSize: 12,
        display: "flex",
        alignItems: "center",
        gap: 10,
        background: selectedLayerId === l.id ? "".concat(BRAND.accent, "22") : "transparent",
        border: "1px solid ".concat(selectedLayerId === l.id ? BRAND.accent : "transparent")
      },
      onMouseEnter: function onMouseEnter(e) {
        if (selectedLayerId !== l.id) e.currentTarget.style.background = BRAND.surfaceHover;
      },
      onMouseLeave: function onMouseLeave(e) {
        if (selectedLayerId !== l.id) e.currentTarget.style.background = "transparent";
      }
    }, React.createElement("span", {
      style: {
        opacity: 0.5,
        fontSize: 10,
        width: 14
      }
    }, layers.length - idx), React.createElement("span", {
      style: {
        fontSize: 14
      }
    }, l.type === "blur" ? "◈" : l.type === "pixelate" ? "⊞" : l.type === "redact" ? "▬" : l.type === "arrow" ? "↗" : l.type === "text" ? "T" : "✎"), React.createElement("div", {
      style: {
        flex: 1,
        overflow: "hidden",
        textOverflow: "ellipsis",
        whiteSpace: "nowrap",
        fontWeight: selectedLayerId === l.id ? 700 : 500,
        color: selectedLayerId === l.id ? BRAND.text : BRAND.textMuted
      }
    }, l.type.charAt(0).toUpperCase() + l.type.slice(1), l.text ? ": ".concat(l.text) : ""), React.createElement("button", {
      onClick: function onClick(e) {
        e.stopPropagation();
        saveHistory();
        setLayers(function (prev) {
          return prev.filter(function (x) {
            return x.id !== l.id;
          });
        });
        setSelectedLayerId(null);
      },
      style: {
        background: "transparent",
        border: "none",
        color: BRAND.textMuted,
        cursor: "pointer",
        padding: 4,
        fontSize: 10
      }
    }, "✕"));
  }).reverse())), React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 8
    }
  }, React.createElement("button", {
    onClick: copyToClipboard,
    style: _objectSpread(_objectSpread({}, btnStyle(BRAND)), {}, {
      width: "100%",
      justifyContent: "center"
    })
  }, "📋 Copy to Clipboard"), React.createElement("button", {
    onClick: function onClick() {
      return exportImage("png");
    },
    style: _objectSpread(_objectSpread({}, btnStyle(BRAND, false, true)), {}, {
      width: "100%",
      justifyContent: "center"
    })
  }, "⬇ Export PNG"), React.createElement("button", {
    onClick: function onClick() {
      return exportImage("jpg");
    },
    style: _objectSpread(_objectSpread({}, btnStyle(BRAND)), {}, {
      width: "100%",
      justifyContent: "center"
    })
  }, "⬇ Export JPG")))),
  // Bottom bar
  React.createElement("div", {
    style: {
      height: 28,
      background: BRAND.surface,
      borderTop: "1px solid ".concat(BRAND.border),
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      padding: "0 16px",
      fontSize: 11,
      color: BRAND.textMuted,
      flexShrink: 0
    }
  }, React.createElement("span", null, "bimp.us — Basic Image Manipulator"), React.createElement("span", null, "Ctrl+Z undo  ·  V select  ·  R redact  ·  B blur  ·  P pixelate  ·  C crop  ·  A arrow  ·  T text  ·  D draw")),
  // 💎 Pro Modal
  proModalOpen && React.createElement("div", {
    style: {
      position: "fixed",
      inset: 0,
      background: "rgba(0,0,0,0.85)",
      backdropFilter: "blur(10px)",
      zIndex: 99999,
      display: "flex",
      alignItems: "center",
      justifyContent: "center"
    },
    onClick: function onClick() {
      return setProModalOpen(false);
    }
  }, React.createElement("div", {
    style: {
      width: 380,
      background: BRAND.surface,
      border: "1px solid ".concat(BRAND.border),
      borderRadius: 16,
      padding: 32,
      boxShadow: "0 20px 40px rgba(0,0,0,0.4)"
    },
    onClick: function onClick(e) {
      return e.stopPropagation();
    }
  }, React.createElement("div", {
    style: {
      fontSize: 24,
      fontWeight: 800,
      marginBottom: 8,
      color: BRAND.accent
    }
  }, "Bimp.us Pro"), React.createElement("div", {
    style: {
      fontSize: 14,
      color: BRAND.textMuted,
      marginBottom: 24,
      lineHeight: 1.5
    }
  }, "Enter your license key to unlock Smart Redact, Batch Processing, and premium mockups."), React.createElement("input", {
    type: "text",
    placeholder: "XXXX-XXXX-XXXX-XXXX",
    value: inputProKey,
    onChange: function onChange(e) {
      return setInputProKey(e.target.value.toUpperCase());
    },
    style: {
      width: "100%",
      background: BRAND.bg,
      border: "1px solid ".concat(BRAND.border),
      borderRadius: 8,
      padding: "12px 16px",
      color: BRAND.accent,
      fontSize: 14,
      fontFamily: "monospace",
      marginBottom: 16,
      outline: "none"
    }
  }), React.createElement("button", {
    onClick: function onClick() {
      return verifyProKey(inputProKey);
    },
    disabled: verifying || inputProKey.length < 5,
    style: _objectSpread(_objectSpread({}, btnStyle(BRAND, verifying || inputProKey.length < 5, true)), {}, {
      width: "100%",
      height: 44,
      justifyContent: "center",
      fontSize: 14,
      color: "#000"
    })
  }, verifying ? "Verifying..." : "Activate Pro"), React.createElement("div", {
    style: {
      marginTop: 24,
      textAlign: "center"
    }
  }, React.createElement("a", {
    href: "https://bimpus.lemonsqueezy.com/my-orders",
    target: "_blank",
    style: {
      color: BRAND.textMuted,
      fontSize: 12,
      textDecoration: "underline"
    }
  }, "Lost your key? Restore purchase")))));
}
var root = ReactDOM.createRoot(document.getElementById("root"));
root.render(React.createElement(BasimpEditor));
